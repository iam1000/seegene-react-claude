# XmlSerializer Methods

| Method | Description |
|--------|-------------|
| `serializeToString()` | DOMDocument 오브젝트를 해석해 XML 문자열을 생성하고 반환하는 메소드입니다. |

---

## serializeToString

DOMDocument 오브젝트를 해석해 XML 문자열을 생성하고 반환하는 메소드입니다.

**Syntax**
```javascript
XmlSerializer.serializeToString( objDOM )
```

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `objDOM` | Object | XML 문자열로 변환하려는 DOMDocument 오브젝트를 설정합니다. |

**Return**: `String` — DOMDocumnet 오브젝트 해석에 성공하면 XML 문자열을 반환합니다.

---
