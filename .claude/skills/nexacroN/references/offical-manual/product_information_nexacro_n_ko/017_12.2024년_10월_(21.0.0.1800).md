---
title: "12.2024년 10월 (21.0.0.1800)"
source: "https://docs.tobesoft.com/product_information_nexacro_n_ko/a05df95bc6850332"
crawled_at: "2026-01-09T10:58:15.704Z"
---

# 12.2024년 10월 (21.0.0.1800)

Nexacro N / 모든 매뉴얼에서 검색제품 정보이전 릴리스 노트12.2024년 10월 (21.0.0.1800)article

# 12.2024년 10월 (21.0.0.1800)

## [Download PDF](https://docs.tobesoft.com/r/pdf/download_chapter/fa466c9bb1a71ea9/a05df95bc6850332)

playlist\_play

12.1버전정보

12.2신규기능

12.3오류수정

12.3.1기본 컴포넌트 / 오브젝트

Application

Calendar

ChildFrame

Combo

Dataset

Div

Edit

ExcelExportObject

FileUpload/FileUpTransfer

Form

Grid

ImageViewer

ListBox

MaskEdit

MultiCombo

Tab

Textarea

WebView

기타 (컴포넌트 / 오브젝트)

12.3.2DeviceAPI

12.3.3접근성(Accessibility)

센스리더 사용 환경 오류

12.3.4넥사크로 스튜디오

12.3.5기타

12.4도움말 수정 및 추가

# 12.1버전정보

| Product Version | 21.0.0.1800 |
| --- | --- |
| File Version | 2024.10.29.1 |

- 사용 환경이나 브라우저 기능 차이 또는 제품 제약으로 발생할 수 있는 현상은 제품제약사항을 참고해주세요.
 
- 새로운 기능, 변경된 기능 목록에서 버전별 새로운 기능, 변경된 기능 목록을 확인할 수 있습니다.

# 12.2신규기능

1. **\[접근성\] html에 Windows NRE 환경을 임베디드 시 tab 키로 포커스 순환을 하도록 기능을 추가했습니다.(RP 100907)**
 

| 설명 |
| --- |
| WRE 환경에서 embedded 여부를 판단하기 위해 window 전영 객체를 사용하는 로직을 API로 분리하여 window 객체 유무를 판단하도록 수정했습니다. |

# 12.3오류수정

## 12.3.1기본 컴포넌트 / 오브젝트

### Application

| RP | 설명 |
| --- | --- |
| 100425 | Application 오브젝트의 exit 메서드를 실행해 앱을 종료한 후 다시 앱을 실행했을 때 onorientationchange 이벤트가 발생하지 못하는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |

### Calendar

| RP | 설명 |
| --- | --- |
| 100947 | Calendar 컴포넌트의 dateformat 속성값 설정 시 월을 "MMMM"으로 설정하고 value 속성값을 설정한 상태에서 앱 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 100454 | Calendar 컴포넌트에서 일본어 IME의 Input Mode를 "Hiragana"로 설정한 상태에서, 숫자 키패드로 날짜를 입력하고 왼쪽 방향키를 눌러 텍스트 맨 앞으로 이동한 후 숫자를 입력하면, 캐럿이 다른 위치(텍스트 오른쪽 끝)로 이동해서 잘못된 위치에 입력되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 100219 | Calendar 컴포넌트에 포커스가 있는 상태에서 alert 메서드를 실행한 후 alert 창을 닫고 Calendar 컴포넌트에 값 입력 시도 시 값을 입력하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 96962 | Calendar 컴포넌트의 datepicker 컨트롤이 표시될 때 잘못된 위치에 표시되는 오류를 수정했습니다(iPadOS 운영체제 WRE 실행 시 발생한 오류입니다. Safari 브라우저에서 "데스크탑 웹 사이트 요청" 기능 선택 후 화면을 확대한 상태에서 실행 시 발생한 오류입니다). |

### ChildFrame

| RP | 설명 |
| --- | --- |
| 100766 | ChildFrame 오브젝트의 dragmovetype 속성값이 "normal"인 경우 TitleBar 가 있는 상태에서 Form 영역 드래그 시 ChildFrame 오브젝트가 이동하는 오류를 수정했습니다. |
| 100708 | ChildFrame 오브젝트의 resizable 속성값이 true인 경우 generate 된 일부 HTML 태그 id가 "undefined"로 처리되는 오류를 수정했습니다. |
| 100421 | VFrameSet 또는 HFrameSet 오브젝트 내 ChildFrame 오브젝트를 2개 이상 추가하고 ChildFrame 오브젝트를 최소화, 최대화 시 나머지 ChildFrame 오브젝트가 재배치되지 못하는 오류를 수정했습니다. |
| 100349 | 기기 방향 전환 시 ChildFrame 오브젝트의 onorientationchange 이벤트가 불규칙하게 발생하지 못하는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |

### Combo

| RP | 설명 |
| --- | --- |
| 100560 | Combo 컴포넌트의 dropbutton 컨트롤 클릭 시 dropbutton 컨트롤이 화면에 표시되지 않는 오류를 수정했습니다(윈도우 운영체제 NRE 실행시 발생한 오류입니다. 64비트 버전 NRE에서 실행 환경에 따라 불규칙하게 발생한 오류입니다). |
| 99847 | Combo 컴포넌트의 innerdataset 속성값으로 설정된 Dataset 오브젝트의 keystring 속성값으로 정렬 방식을 변경한 경우 변경된 정렬 순서가 Combo 컴포넌트에 적용되지 못하는 오류를 수정했습니다. |

### Dataset

| RP | 설명 |
| --- | --- |
| 100264 | Grid 컴포넌트의 selecttype 속성값을 "multirow", "area", "multiarea" 중 하나로 설정한 상태에서 바인딩된 Dataset 오브젝트의 canrowposchange 이벤트 핸들러 함수 내에서 system.showModalWindow 또는 system.showModalSync 메서드 실행한 후 띄운 창을 닫을 때 canrowposchange 이벤트가 발생하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### Div

| RP | 설명 |
| --- | --- |
| 100649 | Div 컴포넌트의 formscrollbartype 속성값이 "none none"이고 formscrolltype 속성값이 "both"인 경우 화면 스크롤 동작을 하지 못하는 오류를 수정했습니다(Android, iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 100496 | Div 컴포넌트의 formscrolltype 속성값을 "both"로 설정하고 앱 실행 후 url 속성값을 변경한 후 formscrolltype 속성값을 "none"으로 변경했을 때 스크롤이 활성화되는 오류를 수정했습니다(iOS 운영체제에서 발생한 오류입니다). |
| 100301 | Div 컴포넌트에서 스크롤 동작 시 인디케이터바와 스크롤바가 같이 표시되는 오류를 수정했습니다(macOS 운영체제 Safari 브라우저에서 실행 시 발생한 오류입니다. 21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 99741 | Div 컴포넌트에서 로딩한 Form에 있는 ListBox 컴포넌트의 onitemclick 이벤트 핸들러 함수 내에서 Div 컴포넌트의 url 속성값을 변경하는 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |

### Edit

| RP | 설명 |
| --- | --- |
| 100634 | 앱 실행 후 마우스 휠을 사용해 화면 배율을 변경한 후 showModal 메서드로 실행한 Form 오브젝트의 onload 이벤트 핸들러 함수 내에서 Edit 컴포넌트의 setFocus 메서드 실행 시 동작하지 않는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100515 | System 오브젝트의 showModalSync 메서드로 띄운 팝업화면에서 스페이스키 입력 시 팝업화면을 닫도록 이벤트 함수를 작성하고 실행하면 부모화면의 Edit 컴포넌트에 스페이스값이 입력되는 오류를 수정했습니다. |
| 100512 | Edit 컴포넌트의 password 속성값이 true이고 displaynulltext 속성값을 설정하고 value 속성을 데이터 바인딩한 상태에서 왼쪽 마우스 버튼 클릭 시 캐럿이 표시되지 못하는 오류를 수정했습니다. |
| 100311100215 | Noto Sans 폰트 사용 시 Edit 컴포넌트에 표시된 텍스트 하단이 잘려 보이는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100183 | Edit 컴포넌트의 maxlength 속성값을 설정한 상태에서 일본어 IME의 Input Mode를 "Hiragana"로 변경해서 정상적으로 입력된 텍스트가 변환되지 못하고 텍스트 편집 영역이 초기화되는 오류를 수정했습니다(윈도우 운영체제 WRE 실행 시 발생한 오류입니다. Chrome, Edge 브라우저 언어 설정이 영어 또는 한국어인 경우 발생한 오류입니다 .21.0.0.01700 이후 버전에서 실행 시 발생한 오류입니다). |
| 100058 | 앱 실행 후 Edit 컴포넌트 텍스트 편집 영역 터치 동작 시 키패드가 올라갔다가 내려가는 오류를 수정했습니다(Android 운영체제 WRE 실행 시 발생한 오류입니다. 삼성 브라우저, Edge 브라우저 실행 시 발생한 오류입니다). |

### ExcelExportObject

| RP | 설명 |
| --- | --- |
| 100670 | Grid 컴포넌트의 locale 속성값이 설정되지 않은 경우 ExcelExportObject 오브젝트의 exportData 메서드 실행 시 브라우저에 설정된 locale이 적용되지 못하는 오류를 수정했습니다.(사용자 브라우저 환경에 따라 "언어[_국가]" locale 형식에서 "_국가" 코드가 누락되는 경우 발생하는 오류입니다. "_국가" 코드가 없는 경우 적절한 값을 설정해 처리합니다). |

### FileUpload/FileUpTransfer

| RP | 설명 |
| --- | --- |
| 100830 | FileDialog 오브젝트에서 선택한 파일을 FileUpTransfer 오브젝트의 upload 메서드를 실행해 업로드 시 스크립트 에러가 발생하는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다. 이미지가 아닌 파일 선택 시 발생한 오류입니다). |
| 100769 | FileUpload 컴포넌트의 fileuploaditem 컨트롤 내 fileuploaditembutton 컨트롤의 border-radius, -nexa-border 스타일 설정 시 버튼 UI 일부가 잘린 것처럼 표시되는 오류를 수정했습니다. |

### Form

| RP | 설명 |
| --- | --- |
| 100920 | Form 오브젝트의 stepcount 속성값을 스크립트에서 설정한 후 화면 전환 시 전환되는 화면에 PopupDiv 컴포넌트가 포함된 경우 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제, Android 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100492 | Div 컴포넌트의 url 속성값을 변경해 로딩된 Form 오브젝트에 포함된 ImageViewer 컴포넌트의 text 속성값이 화면에 표시되지 못하는 오류를 수정했습니다. |
| 96544 | Form 오브젝트의 cssclass 속성값에 2개 이상의 컨트롤에 대한 Class Selector를 설정하는 경우 적용되지 못하는 오류를 수정했습니다. |

### Grid

| RP | 설명 |
| --- | --- |
| 100656 | Grid 컴포넌트의 scrollpixel 속성값이 'all"인 경우 마우스 휠 스크롤 동작 시 wheelscrollrow 속성값 단위로 스크롤이 동작하지 못하는 오류를 수정했습니다. |
| 100552 | Grid 컴포넌트의 autofittype 속성값이 "col", autosizingtype 속성값이 "row"일 때 로딩 시 Grid 컴포넌트의 크기나 로딩 후 Grid 컴포넌트의 크기에 변경이 생기는 경우 Grid 컴포넌트의 크기가 특정 조건일 때 Head 밴드 영역 크기가 잘못 표시되는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 100476 | Grid 컴포넌트에서 Cell 영역을 터치해서 oncellclick 이벤트가 발생한 후 마우스 또는 트랙패드로 스크롤 이동 후 다른 Cell 영역 클릭 동작 시 oncellclick 이벤트가 발생하지 못하는 오류를 수정했습니다(윈도우 운영체제 WRE 실행 시 발생한 오류입니다. Microsoft Surface 기기 사용 시 발생한 오류입니다). |
| 100455 | Grid 컴포넌트의 autofittype 속성값이 "col", autosizingtype 속성값이 "row", treeasynctoggle 속성값이 true, treeusebutton 속성값이 "noclick"인 경우 앱 실행 후 트리를 확장했을 때 Wait Cursor가 표시되고 사라지지 않는 오류를 수정했습니다(트리가 확장되지 않은 상태일 때는 Grid 컴포넌트 영역 내 모든 데이터가 표시되고 트리 확장 시 Grid 컴포넌트의 스크롤 영역이 활성화되는 경우 발생한 오류입니다). |
| 100354 | Grid 컴포넌트의 insertContentsCol 메서드 실행 시 또는 showselection 속성값을 true로 설정하고 deleteContentsCol 메서드를 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 100169 | Grid 컴포넌트의 enableredraw 속성값이 변경될 때 스크롤 위치가 이전과 다르게 표시되는 오류를 수정했습니다(21.0.0.800 이후 버전에서 발생한 오류입니다). |
| 99993 | Grid 컴포넌트의 oncolumnchanged 이벤트 핸들러 함수 내에서 ChildFrame 오브젝트의 showModal 메서드 또는 system.showModalSync 메서드 실행 후 띄운 창을 닫고 enableredraw 속성값을 false에서 true로 변경하는 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 99986 | Grid 컴포넌트의 enableredraw 속성값을 false로 설정한 상태에서 setFormatRowProperty, setRealRowSize 메서드 실행 후 enableredraw 속성값을 true로 변경하면 setFormatRowProperty 메서드에서 설정한 값이 적용되지 못하는 오류를 수정했습니다. |
| 99982 | Grid 컴포넌트의 enableredraw 속성값을 false로 설정한 상태에서 autoSizeRow 메서드 실행 후 enableredraw 속성값을 true로 변경하면 autoSizeRow 메서드에서 설정한 값이 적용되지 못하는 오류를 수정했습니다. |
| 99957 | Grid 컴포넌트의 onsize 이벤트 핸들러 함수에서 autosizingtype 속성값을 변경하고 setRealRowSize 메서드를 실행하는 경우 Grid 컴포넌트 크기 변경 시 autosizingtype 속성값이 반영되지 못하거나 setRealRowSize 메서드가 실행되지 못하는 오류를 수정했습니다. |
| 98295 | Grid 컴포넌트의 autosizebandtype 속성값이 "allband" 또는 "head"이고 autosizingtype 속성값이 "both" 또는 "row"인 경우 binddataset 속성값을 설정하지 않으면 head 밴드 영역 자동 크기 조절이 되지 않은 오류를 수정했습니다. |
| 96433 | 기본 테마에서 CheckBoxControl의 -nexa-icon 속성 설정 부분을 삭제하고 사용자 XCSS에서 -nexa-icon을 설정한 경우 Grid 컴포넌트의 autosizingtype 속성값이 "none"이 아니고 Cell 오브젝트의 displaytype 속성값이 "checkboxcontrol" 또는 "treeitemcontrol"인 경우 컴포넌트 로딩 시 스크립트 에러가 발생하는 오류를 수정했습니다(윈도우 운영체제 WRE 실행 시 발생한 오류입니다). |

### ImageViewer

| RP | 설명 |
| --- | --- |
| 100934 | ImageViewer 컴포넌트의 fittocontents 속성값을 "none"이 아닌 값으로 설정한 경우 Form 로딩 시 로딩되는 이미지 크기에 따라 fittocontents 속성이 적용되지 못하는 오류를 수정했습니다. |

### ListBox

| RP | 설명 |
| --- | --- |
| 100649 | ListBox 컴포넌트의 scrollbartype 속성값이 "none none"이고 scrolltype 속성값이 "both"인 경우 화면 스크롤 동작을 하지 못하는 오류를 수정했습니다(Android, iOS 운영체제 WRE 실행 시 발생한 오류입니다). |

### MaskEdit

| RP | 설명 |
| --- | --- |
| 100220 | MaskEdit 컴포넌트에 포커스가 있는 상태에서 alert 메서드를 실행한 후 alert 창을 닫고 MaskEdit 컴포넌트에 값 입력 시도 시 값을 입력하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100204100203 | MaskEdit 컴포넌트의 padding 속성값을 설정한 경우 숫자값 입력 시 보이는 캐럿 위치와 실제 값이 편집되는 위치가 달라지는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1700 이후 버전에서 발생한 오류입니다). |
| 100202 | 키보드 Insert 키가 Overtype mode인 경우 MaskEdit 컴포넌트 텍스트 입력 영역을 블록으로 선택한 후 값 입력 시 첫 번째 입력값이 오른쪽에 표시되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100201 | 키보드 Insert 키가 Overtype mode인 경우 MaskEdit 컴포넌트 텍스트 입력 영역에 값 입력 후 포커스 이동 시 입력값이 사라지는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100156 | MaskEdit 컴포넌트에 입력된 텍스트의 길이가 MaskEdit 컴포넌트 텍스트 입력 영역보다 긴 경우 텍스트 오른쪽 끝 부분이 잘려 표시되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1600 이후 버전에서 발생한 오류입니다). |

### MultiCombo

| RP | 설명 |
| --- | --- |
| 100955 | MultiCombo 컴포넌트 multicombolist에서 readonlycolumn 값이 false인 아이템을 선택한 후 readonlycolumn 값이 true인 아이템으로 드래그 앤 드롭하면 readonlycolumn 값이 true인 아이템이 선택되는 오류를 수정했습니다. |
| 100952 | MultiCombo 컴포넌트의 type 속성값을 "dropdown"이 아닌 값으로 설정하고 필터링된 목록에 readonlycolumn 값이 true인 아이템이 포함된 경우 방향키를 입력해 아이템을 이동하면 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 100951 | MultiCombo 컴포넌트의 type 속성값을 "dropdown"이 아닌 값으로 설정하고 taborder 속성값이 이전, 이후인 컴포넌트가 텍스트 편집 영역을 가지고 있는 컴포넌트인 경우 Tab 키 입력 시 MultiCombo 다음 컴포넌트로 이동하지 못하는 오류를 수정했습니다. |
| 100950 | MultiCombo 컴포넌트의 type 속성값이 "search"인 경우 필터링된 목록 중 readonlycolumn 값이 true인 값이 선택되는 오류를 수정했습니다. |
| 100494 | MultiCombo 컴포넌트의 아이템을 1개 이상 선택한 상태에서 바인딩된 Dataset 오브젝트의 clearData 메서드 실행 시 MultiCombo 컴포넌트에서 선택한 항목에 대한 정보가 일부 초기화되지 않고 남아있는 오류를 수정했습니다. |
| 100493 | MultiCombo 컴포넌트의 아이템을 1개 이상 선택한 상태에서 바인딩된 Dataset 오브젝트의 Row 데이터를 추가하는 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 10036297526 | MultiCombo 컴포넌트에서 readonlycolumn 값이 true인 값이 터치 동작으로 선택이 되는 오류를 수정했습니다(모바일 운영체제 WRE 실행 시 발생한 오류입니다). |

### Tab

| RP | 설명 |
| --- | --- |
| 99959 | Tab 컴포넌트의 nextbutton 컨트롤이 활성화된 경우 nextbutton 컨트롤을 클릭해 표시되는 tabbuttonitem 컨트롤 클릭 시 onchanged 이벤트 핸들러 함수 내에서 Tab 컴포넌트의 height 속성값을 변경하는 경우 클릭한 tabbuttonitem 컨트롤 위치가 변경되는 오류를 수정했습니다. |

### Textarea

| RP | 설명 |
| --- | --- |
| 100520 | TextArea 컴포넌트의 wordWrap 속성값을 "none"으로 설정했을 때 속성값이 적용되지 못하고 테마에 설정한 -nexa-word-wrap 속성값이 적용되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100381100335 | TextArea 컴포넌트의 maxlength 속성값을 설정한 경우 개행문자가 포함된 텍스트 붙여 넣기 시 개행문자를 포함해 길이를 계산하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100311100215 | Noto Sans 폰트 사용 시 TextArea 컴포넌트에 표시된 마지막 줄 텍스트 하단이 잘려 보이고 캐럿 위치가 텍스트 라인보다 위에 표시되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### WebView

| RP | 설명 |
| --- | --- |
| 100704 | WebView 컴포넌트에서 로딩한 콘텐츠에서 녹음 권한 요청 시 처리되지 못하는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100253 | WebView 컴포넌트의 onusernotify 이벤트 핸들러 함수 내에서 system.showModalSync 메서드 실행 시 띄운 창으로 포커스가 이동하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### 기타 (컴포넌트 / 오브젝트)

| RP | 설명 |
| --- | --- |
| 100599100579 | nexacro.open 메서드 실행 후 띄운 창을 닫는 동작을 반복했을 때 메모리 사용량이 증가하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 100289 | Screen.userzoom 속성값을 true로 설정한 경우 브라우저 화면을 아래로 당기면 새로고침되는 오류를 수정했습니다(iOS 운영체제 Safari 브라우저에서 실행 시 발생한 오류입니다). |
| 96482 | XCSS에서 컴포넌트의 -nexa-icon 속성값을 "none"으로 설정한 경우 기본 테마에 설정된 값이 반영되는 오류를 수정했습니다. |

## 12.3.2DeviceAPI

| RP | 설명 |
| --- | --- |
| 100961 | BluetoothLE 오브젝트의 scanStart 메서드 실행 시 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 64비트 앱 실행 시 발생한 오류입니다). |
| 100878 | Camera 오브젝트의 takePicture 메서드를 실행하지 못하는 오류를 수정했습니다(Android 9 운영체제에서 실행 시 발생한 오류입니다). |
| 100163 | LiteDBConnection 오브젝트의 open 메서드 실행 후 연결 실패 시 LiteDBErrorEventInfo 오브젝트의 errormsg 속성값이 잘못된 값으로 처리되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

## 12.3.3접근성(Accessibility)

| RP | 설명 |
| --- | --- |
| 100865 | Grid 컴포넌트에서 마우스 왼쪽 버튼으로 셀 클릭 시 포커스가 이동하지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 100124100123 | Edit 컴포넌트의 accessibilitylabel 속성값을 설정하고 value 속성값이 있는 경우 포커스 이동 시 accessibilitylabel 속성값만 두 번 읽는 오류를 수정했습니다. |
| 100094 | Calendar 컴포넌트로 이동 시 text 속성값으로 적용된 형식대로 읽지 못하는 오류를 수정했습니다. |
| 99903 | HTML IFRAME 태그 내에서 넥사크로 앱을 로딩하는 경우 화면 로딩 시 HTML 태그 내 설정된 button 태그를 무시하고 넥사크로 앱의 첫 번째 컴포넌트로 포커스가 이동하는 오류를 수정했습니다. |
| 99774 | Environment.tabkeycirculation 속성값을 "form,cycle"으로 설정한 경우 앱을 HTML IFRAME 내에서 실행했을 때 앱을 빠져나가지 못하는 오류를 수정했습니다. |

### 센스리더 사용 환경 오류

접근성 관련 오류 중 센스리더 사용 환경에서 가상 커서를 사용으로 설정하고 구글 크롬 브라우저 실행 시 발생한 오류를 따로 분류해 정리합니다.

센스리더 사용 환경이 다른 경우에는 실행 환경을 별도 기재했습니다.

| RP | 설명 |
| --- | --- |
| 100833100832100790 | Div 컴포넌트 내 첫 번째 컴포넌트 접근 시 Div 컴포넌트의 accessibilitylabel 정보를 같이 읽는 오류를 수정했습니다(센스리더 8.5.2 이상 버전에서 실행 시 발생한 오류입니다). |
| 100807 | Static 컴포넌트에 초점을 이동하지 못하는 오류를 수정했습니다(센스리더 8.5.2 이상 버전에서 실행 시 발생한 오류입니다). |
| 100794 | TextArea 컴포넌트의 readonly 속성값이 true인 경우 방향키 입력 시 TextArea 컴포넌트에 초점을 이동하지 못하는 오류를 수정했습니다(센스리더 8.5.2 이상 버전에서 실행 시 발생한 오류입니다). |
| 100776 | 이전 컴포넌트에서 Edit 컴포넌트로 빠르게 접근 시 accessibilitylabel 정보를 읽지 못하는 오류를 수정했습니다(센스리더 8.5.2 이상 버전에서 실행 시 발생한 오류입니다). |
| 100093 | TextArea 컴포넌트로 이동 시 컴포넌트의 accessibilitylabel, accessibilityaction, accessibilitydescription 속성값을 두 번 읽는 오류를 수정했습니다. |
| 9979599837 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "checkboxcontrol"이고 edittype 속성값이 "checkbox"인 경우 가상커서 이동 시 칼럼값을 읽지 못하는 오류를 수정했습니다. |

## 12.3.4넥사크로 스튜디오

| RP | 설명 |
| --- | --- |
| 101027 | ListViewContents Editor에서 Cell을 삭제하면 화면에는 삭제된 상태로 표시되지만 탭을 전환하거나 저장할 때 실제 소스 데이터에 반영되지 않는 오류를 수정했습니다. |
| 100997 | Tab 컴포넌트 TabpageControl 오브젝트 편집 상태에서 Undo 명령 실행 시 비정상 종료되는 오류를 수정했습니다. |
| 100979 | 디자인 창에서 두 개의 컴포넌트를 PositionBase로 설정한 상태에서 한쪽 컴포넌트를 삭제하면서 마우스 포인터를 이동하면 비정상 종료되는 오류를 수정했습니다. |
| 100972 | Options에서 [Form Design > Guide > Snap to dots when moving component] 옵션을 체크하고 디자인 화면을 확대 또는 축소한 상태에서 컴포넌트를 선택하고 Ctrl+Shift 키를 누르고 드래그해서 컴포넌트를 복사하면 컴포넌트의 위치가 원본 컴포넌트의 top 또는 left 속성값과 달라지는 오류를 수정했습니다. |
| 100958 | Outlineview 패널을 활성화한 상태에서 디자인 창에서 Ctrl+드래그 앤 드롭 동작으로 컴포넌트를 복사한 경우 새로 생성된 컴포넌트에 포커스가 표시되지 못하는 오류를 수정했습니다. |
| 100953 | DataObject 오브젝트의 setEventHandlerLookup 메서드명에 오타가 포함된 오류를 수정했습니다. |
| 100929 | 스크립트 창에서 Graphics 컴포넌트의 removeEvent 메서드가 표시되지 못하는 오류를 수정했습니다. |
| 100904 | TabpageControl 오브젝트를 복사해서 Form 영역에 붙여 넣기가 되는 오류를 수정했습니다. |
| 100635 | 컴포넌트 선택 후 F2 키를 입력해 InplaceEdit를 활성화한 상태에서 Undo 동작 실행 시 비정상 종료되는 오류를 수정했습니다. |
| 100499 | Invisible Object 패널 내 Input filter text의 드롭다운 목록이 펼쳐진 상태에서 드롭다운 버튼 클릭 시 드롭다운 목록이 닫혔다가 다시 펼쳐지는 오류를 수정했습니다. |
| 100495 | Invisible Object 패널에서 항목을 선택하지 않은 상태에서 표시된 콘텍스트 메뉴에 "Copy ID" 항목이 활성화되는 오류를 수정했습니다. |
| 100491 | 디자인 화면에서 컨테이너 컴포넌트 영역 내에 Dataset 오브젝트나 DataObject 오브젝트를 드래그해서 생성한 후 Undo 실행 시 동작하지 않는 오류를 수정했습니다. |
| 100443 | XCSS 편집창에서 Predefine 탭을 열고 아이템 선택 시 비정상 종료되는 오류를 수정했습니다(64비트 버전 넥사크로 스튜디오 실행 시 발생한 오류입니다). |
| 100358 | 스크립트 내 함수 인수 일부를 주석으로 막은 경우 마이그레이션 시 주석으로 막은 인수가 삭제되는 오류를 수정했습니다. |
| 100278 | 디자인 화면에서 Tab 컴포넌트와 TabpageControl 오브젝트를 선택한 상태에서 마우스 드래그로 이동했을 때 비정상 종료되는 오류를 수정했습니다. |
| 100137 | nexacrostudio.request 메서드 실행 시 requestType 파라미터값을 requesttype.forms로 설정한 경우 xfdl 파일 스크립트 내 \" 문자가 포함된 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 99582 | 스크립트 작성 시 const 구조 분해 할당 선언문을 사용하면 스크립트 에러로 처리되는 오류를 수정했습니다. |
| 99415 | 스크립트 작성 시 화살표 함수가 문법에 맞지 않게 사용된 경우 스크립트 에러로 처리하지 못하는 오류를 수정했습니다. |
| 98123 | Form 디자인 화면에서 Copy & Add Layout 기능 실행 시 소스 코드 내에 컴포넌트의 Event 정보가 복사되는 오류를 수정했습니다. |
| 97522 | Service 하위 디렉터리에 있는 Form을 ChildFrame 오브젝트의 formurl 속성값으로 설정한 경우 최초 generate 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 97314 | Merge 옵션에서 "Merge XCSS file used by Application" 항목 체크 시 Application에 사용하는 XCSS 파일을 하나의 파일로 병합하지 못하는 오류를 수정했습니다(21.0.0.800 이후 버전에서 발생한 오류입니다). |
| 96430 | XCSS 편집기에서 "Add Selector" 기능 선택 후 "GridCellControl" 항목 추가 시 스크립트 에러가 발생하는 오류를 수정했습니다. |

## 12.3.5기타

| RP | 설명 |
| --- | --- |
| 100725100724100717 | 폴더블 기기에서 앱 실행 후 기기를 접었을 때 화면이 깨져서 표시되거나 비정상 종료되는 오류를 수정했습니다(Android NRE 실행 시 발생한 오류입니다. 갤럭시 폴드 3 기기에서 발생한 오류입니다). |
| 100379 | Debug 모드로 빌드한 앱 실행 시 화면이 검은색으로 표시되는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100259100236100231100221100214100212100200100197100196100177 | 아래 항목에 대한 보안 취약점 관련 코드를 보완했습니다.- 오류 메시지를 통한 정보 노출- 초기화되지 않는 변수 사용- null 포인트 역참조- 부적절한 자원 해제- 경로 조작 및 자원 삽입- 부적절한 인증서 유효성 검증- Private 배열에 Public 데이터 할당- 부적절한 예외처리 |
| 98323 | Service CacheLevel 값을 "session"으로 설정한 경우 Form 오브젝트에서 include 하는 xjs 파일은 "dynamic"으로 설정한 것처럼 동작하는 오류를 수정했습니다. |

# 12.4도움말 수정 및 추가

| RP | 설명 |
| --- | --- |
| 101001 | 컴포넌트의 accessibilityenable 속성 도움말에 제약 관련 설명을 추가했습니다. |
| 100823 | DataObjectErrorEventInfo, DataObjectEventInfo, DataObjectLoadEventInfo 오브젝트의 fromreferenceobject 속성명에 오타가 포함된 오류를 수정했습니다. |
| 100588 | Environment 오브젝트의 datasetloadtype 속성 설명을 보완했습니다. |
| 100475 | Grid 컴포넌트 내 Structure 이미지에 누락된 항목을 보완했습니다. |
| 100473 | Tab 컴포넌트의 removeTabpage 메서드 설명에 Form 오브젝트의 onbeforeclose, onclose 이벤트 관련된 내용 설명을 보완했습니다. |
| 100449 | Form 오브젝트의 onbeforeclose, onclose, onactivate, ondeactivate 이벤트 항목에 visible, enable 속성값에 따른 이벤트 발생에 대한 설명을 추가했습니다. |
| 99774 | Environment accessibilityfirstovermessage, accessibilitylastovermessage 속성 항목에 HTML IFRAME 내 로딩 시 동작 설명을 추가했습니다. |

TOBESOFT ©