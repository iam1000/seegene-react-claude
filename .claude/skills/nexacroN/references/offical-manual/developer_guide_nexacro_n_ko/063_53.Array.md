---
title: "53.Array"
source: "https://docs.tobesoft.com/developer_guide_nexacro_n_ko/cb4d7b81dc99a66a"
crawled_at: "2026-01-09T10:58:15.106Z"
---

# 53.Array

Nexacro N / 모든 매뉴얼에서 검색컴포넌트 활용 워크북자바스크립트 표준 내장 객체53.Arrayarticle

# 53.Array

## [Download PDF](https://docs.tobesoft.com/r/pdf/download_chapter/119983296a7271b3/cb4d7b81dc99a66a)

playlist\_play

53.1문자열을 분리해 배열로 만들기

53.1.1예제

53.1.2예제에서 사용한 핵심 기능

53.1.3예제 구현 방법

53.2중복된 요소 제거하기

53.2.1예제

53.2.2예제에서 사용한 핵심 기능

53.2.3예제 구현 방법

53.3숫자 배열 정렬하기

53.3.1예제

53.3.2예제에서 사용한 핵심 기능

53.3.3예제 구현 방법

53.42차원 배열 만들기

53.4.1예제

53.4.2예제에서 사용한 핵심 기능

53.4.3예제 구현 방법

# 53.1문자열을 분리해 배열로 만들기

입력된 문자열을 지정한 길이만큼 잘라 배열로 만듭니다.

## 53.1.1예제

Edit 컴포넌트에 입력된 값을 지정된 길이만큼 잘라 배열로 반환합니다.

![](https://docs.tobesoft.com/r/book_image/get/5436819f5b7789c4?0)

![](https://docs.tobesoft.com/r/book_image/inline/05e7fea258c9cfec_37854e9e4332b1e3)[sample\_array\_01.xfdl](http://demo.nexacro.com/developer_guide/N/index.html?sa=sample_array_01&ma=bef2ec59fae40c15)

## 53.1.2예제에서 사용한 핵심 기능

push

배열에 마지막 값을 추가하는 메소드입니다.

## 53.1.3예제 구현 방법

1. Form 화면 구성하기

Edit 컴포넌트, Button 컴포넌트를 배치합니다.

2. onclick 이벤트 함수 작성하기

버튼 클릭 시 배열을 생성하고 입력된 문자열 길이만큼 반복하면서 배열에 문자열을 잘라서 추가합니다.

this.Button00\_onclick = function(obj:nexacro.Button,e:nexacro.ClickEventInfo)
{
    var arrArray = new Array();
    var sString = this.Edit00.value;
    var nSize = 3;
 for (var i = 0; i < sString.length; i+=nSize)
 { 
 arrArray.push(sString.substr(i, nSize)); 
 }
    this.Edit01.set\_value(arrArray);
};

예제의 경우 실제 배열에는 아래와 같이 값이 들어갑니다.

![](https://docs.tobesoft.com/r/book_image/get/7886c628919c7fb5?0)

3. QuickView로 확인하기

QuickView(Ctrl + F6)로 실행한 후 값을 입력해 확인해봅니다.

# 53.2중복된 요소 제거하기

입력된 문자열을 배열로 바꾸고 중복된 요소를 제거합니다.

## 53.2.1예제

Edit 컴포넌트에 입력된 값을 콤마(,)를 기준으로 잘라서 배열로 변환하고 중복된 요소를 제거한 결과를 반환합니다.

![](https://docs.tobesoft.com/r/book_image/get/3ab54e09387c0638?0)

![](https://docs.tobesoft.com/r/book_image/inline/8c2547b3f1ddf6ff_37854e9e4332b1e3)[sample\_array\_02.xfdl](http://demo.nexacro.com/developer_guide/N/index.html?sa=sample_array_02&ma=bb5412eef7556c72)

## 53.2.2예제에서 사용한 핵심 기능

split

문자열을 구분자에 따라 분할해 배열로 반환합니다.

## 53.2.3예제 구현 방법

1. Form 화면 구성하기

Edit 컴포넌트, Button 컴포넌트를 배치합니다.

2. onclick 이벤트 함수 작성하기

버튼 클릭 시 배열을 생성하고 입력된 문자열 길이만큼 반복하면서 배열에 문자열을 잘라서 추가합니다.

this.Button00\_onclick = function(obj:nexacro.Button,e:nexacro.ClickEventInfo)
{
    var aRtn = this.fn\_distinct(this.Edit00.value);
    this.Edit01.set\_value(aRtn);
};

this.fn\_distinct = function(sParam)
{
 var aData = new Array();
 aData = sParam.split(",");
    
 var aRtn = new Array();
 var bFlag;
 var vDist;
 
 for (var i = 0; i < aData.length; i++)
 {
 vDist = aData\[i\];
 bFlag = false;
 for (var j = 0; j < aRtn.length; j++)
 {
            if (aRtn\[j\] == vDist)
 {
 bFlag = true;
 break;
 }
 }
 if (bFlag == false)
 {
            aRtn\[aRtn.length\] = vDist; 
 }
 }
 return aRtn; 
}

예제에서는 배열에 담긴 모든 요소를 하나하나 비교해가며 중복된 값을 제거했습니다. 배열이 크지 않은 경우에는 큰 차이가 없지만, 크기가 큰 경우에는 배열을 먼저 정렬한 다음 중복된 값을 제거하는 것이 더 빠를 수도 있습니다.

3. QuickView로 확인하기

QuickView(Ctrl + F6)로 실행한 후 값을 입력해 확인해봅니다.

# 53.3숫자 배열 정렬하기

입력된 문자열을 배열로 바꾸고 정렬한 값을 반환합니다.

## 53.3.1예제

Edit 컴포넌트에 입력된 값을 콤마(,)를 기준으로 잘라서 배열로 변환하고 문자열 또는 숫자 기준으로 정렬한 값을 표시합니다.

![](https://docs.tobesoft.com/r/book_image/get/b865aed8b23f01e1?0)

![](https://docs.tobesoft.com/r/book_image/inline/6cb05f192ac378cc_37854e9e4332b1e3)[sample\_array\_03.xfdl](http://demo.nexacro.com/developer_guide/N/index.html?sa=sample_array_03&ma=e2eb660d7424bdfa)

## 53.3.2예제에서 사용한 핵심 기능

sort

정렬된 새로운 배열을 반환합니다. 파라미터에 함수를 지정하면 정렬 순서를 원하는 형식으로 변경할 수 있습니다.

## 53.3.3예제 구현 방법

1. Form 화면 구성하기

Edit 컴포넌트, Button 컴포넌트를 배치합니다.

2. onclick 이벤트 함수 작성하기

버튼 클릭 시 배열을 생성하고 입력된 문자열 길이만큼 반복하면서 배열에 문자열을 잘라서 추가합니다.

this.Button00\_onclick = function(obj:nexacro.Button,e:nexacro.ClickEventInfo)
{
    var arrNo = this.Edit00.value.split(",");
    this.fn\_arrayNoSort(arrNo);    
     this.fn\_arrayStringSort(arrNo);
};

this.fn\_arrayStringSort = function(arrNo)
{
 this.Edit01.set\_value(arrNo.sort());
};

this.fn\_arrayNoSort = function(arrNo)
{
    this.Edit02.set\_value(arrNo.sort(function(a, b){return a-b}));
};

3. QuickView로 확인하기

QuickView(Ctrl + F6)로 실행한 후 값을 입력해 확인해봅니다.

# 53.42차원 배열 만들기

2차원 배열과 유사한 형식을 구현할 수 있습니다. 정확하게는 배열 안에 배열 요소를 배치하는 방식입니다.

## 53.4.1예제

생성할 2차원 배열의 행, 열 갯수를 지정하면 2차원 배열 형태를 생성해서 TextArea 컴포넌트에 표시합니다.

![](https://docs.tobesoft.com/r/book_image/get/d0a1b3412b4ef9b5?0)

![](https://docs.tobesoft.com/r/book_image/inline/3a076bdb4c38a8d7_37854e9e4332b1e3)[sample\_array\_04.xfdl](http://demo.nexacro.com/developer_guide/N/index.html?sa=sample_array_04&ma=699858dcb4a1d5d8)

## 53.4.2예제에서 사용한 핵심 기능

new Array()

new Array()로 Array 오브젝트를 생성하지 않고 \[\]를 대입해주어도 Array 오브젝트를 생성합니다.

## 53.4.3예제 구현 방법

1. Form 화면 구성하기

Edit 컴포넌트, Button 컴포넌트를 배치합니다.

2. onclick 이벤트 함수 작성하기

버튼 클릭 시 배열을 생성하고 입력된 문자열 길이만큼 반복하면서 2차원 배열 형식을 만듭니다.

this.Button00\_onclick = function(obj:nexacro.Button,e:nexacro.ClickEventInfo)
{
    var arrValue = \[\];
    var nLen1 = this.Edit00.value;
    var nLen2 = this.Edit01.value;
    for (var i = 0; i < nLen1; i++)
    {
        arrValue\[i\] = new Array();
        for (var j = 0; j < nLen2; j++)
        {
            arrValue\[i\]\[j\] = " \[" + i + "\]\[" + j + "\]";
        }
    }
    var sRtn = "";
 for (var i = 0; i < nLen1; i++)
 {
 sRtn += arrValue\[i\] + "\\n";
 }    
    this.TextArea00.set\_value(sRtn);
};

3. QuickView로 확인하기

QuickView(Ctrl + F6)로 실행한 후 값을 입력해 확인해봅니다.

TOBESOFT ©