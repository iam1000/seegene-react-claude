---
name: nexacron-calendar
description: >
  NexacroN Calendar 컴포넌트의 전체 API 레퍼런스. 날짜 입력, 포맷, 범위 제한 등을 다룬다.
  NexacroN Calendar 컴포넌트의 Property, Method, Event 상세 API 레퍼런스.
  calendar, cal_, dateformat, editformat, oncloseup, ondropdown 키워드가 포함된 요청에서 트리거된다.
---

# nexacron-calendar

NexacroN Calendar 컴포넌트의 전체 API 레퍼런스. 날짜 입력, 포맷, 범위 제한 등을 다룬다.

## Calendar

- Properties: 84개
- Methods: 57개
- Events: 32개

### 주요 Properties
`-nexa-border`, `-nexa-edge`, `-nexa-padding`, `-nexa-rtl-background-image`, `-nexa-rtl-edge-image`, `accessibilityaction`, `accessibilitydesclevel`, `accessibilitydescription`, `accessibilityenable`, `accessibilitylabel`, `accessibilityrole`, `autoselect`, `autoskip`, `background`, `backgroundcolumn`
  외 69개 → `references/Calendar_properties.md` 참조

### 주요 Methods
`addEvent`, `addEventHandler`, `addEventHandlerLookup`, `bringToFront`, `bringToPrev`, `clearEventHandler`, `destroy`, `dropdown`, `findEventHandler`, `getCaretPos`, `getDay`, `getDayOfWeek`, `getDisplayText`, `getEventHandler`, `getInnerDataset`
  외 42개 → `references/Calendar_methods.md` 참조

### 주요 Events
`canchange`, `onchanged`, `oncloseup`, `oncontextmenu`, `ondayclick`, `ondevicebuttonup`, `ondrag`, `ondragenter`, `ondragleave`, `ondragmove`
  외 22개 → `references/Calendar_events.md` 참조

## 상세 레퍼런스

- `references/Calendar_properties.md` — Calendar 전체 Property 목록
- `references/Calendar_methods.md` — Calendar 전체 Method 목록
- `references/Calendar_events.md` — Calendar 전체 Event 목록
