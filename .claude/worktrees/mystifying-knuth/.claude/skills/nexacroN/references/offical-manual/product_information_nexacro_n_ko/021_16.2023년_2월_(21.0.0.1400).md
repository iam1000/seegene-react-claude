---
title: "16.2023년 2월 (21.0.0.1400)"
source: "https://docs.tobesoft.com/product_information_nexacro_n_ko/0efaa535e07a0bdc"
crawled_at: "2026-01-09T10:58:15.879Z"
---

# 16.2023년 2월 (21.0.0.1400)

Nexacro N / 모든 매뉴얼에서 검색제품 정보이전 릴리스 노트16.2023년 2월 (21.0.0.1400)article

# 16.2023년 2월 (21.0.0.1400)

## [Download PDF](https://docs.tobesoft.com/r/pdf/download_chapter/fa466c9bb1a71ea9/0efaa535e07a0bdc)

playlist\_play

16.1버전정보

16.2신규기능

16.3오류수정

16.3.1기본 컴포넌트 / 오브젝트

Animation

Calendar

DataObject

Dataset

Edit

ExcelExportObject

FileDialog

Form

Grid

ListView

MaskEdit

Sketch

Tab

Textarea

WebView

기타 (컴포넌트 / 오브젝트)

기타 (transaction)

16.3.2접근성(Accessibility)

센스리더 사용 환경 오류

16.3.3QuickCode

16.3.4넥사크로 스튜디오

16.3.5기타

16.4도움말 수정 및 추가

# 16.1버전정보

| Product Version | 21.0.0.1400 |
| --- | --- |
| File Version | 2023.2.28.1 |

- 사용 환경이나 브라우저 기능 차이 또는 제품 제약으로 발생할 수 있는 현상은 제품제약사항을 참고해주세요.
 
- 새로운 기능, 변경된 기능 목록에서 버전별 새로운 기능, 변경된 기능 목록을 확인할 수 있습니다.

# 16.2신규기능

1. **ExcelImportObject에 서버와 테이터 전송 시 사용할 통신형식을 설정하는 속성을 추가했습니다. (RP 96280)**
 

| 설명 |
| --- |
| enumFormat ::= 'xml' | 'ssv' | 'json''xml' : XML 형식으로 데이터를 전송합니다.'ssv' : SSV 형식으로 데이터를 전송합니다.'json' : JSON 형식으로 데이터를 전송합니다.commdataformat 속성값을 설정하지 않으면 "xml" 로 적용됩니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Misc. Objects > ExcelImportObject > Property > commdataformat |

2. **iOS앱의 네이티브 영역에서 넥사크로 로딩이 끝난 시점을 알기 위한 인터페이스를 추가했습니다. (RP 95965)**
 

| 설명 |
| --- |
| AppViewController을 생성할때 넥사크로 로딩이 끝난 시점을 알기 위한 인터페이스를 추가했습니다. |
| 자세한 내용은 "넥사크로 N 앱 배포 가이드"를 확인하시기 바랍니다.http://docs.tobesoft.com/deployment_guide_nexacro_n_ko#6d418204339e1516 |

# 16.3오류수정

## 16.3.1기본 컴포넌트 / 오브젝트

### Animation

| RP | 설명 |
| --- | --- |
| 95493 | Animation 오브젝트의 oncomplete 이벤트 핸들러 함수 내에서 Form 오브젝트의 close 메소드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |

### Calendar

| RP | 설명 |
| --- | --- |
| 96295 | Calendar 컴포넌트 텍스트 편집 영역에 날짜값을 입력하고 calendarspinupbutton, calendarspindownbutton을 조작해서 onspin 이벤트 발생 시 CalendarSpinEventInfo 오브젝트의 prevalue 속성값과 Calendar 컴포넌트의 value 속성값이 잘못된 값으로 처리되는 오류를 수정했습니다. |
| 96256 | Calendar 컴포넌트의 type 속성값이 "spin"일 때 calendaredit 편집 상태에서 상하 방향키 입력 시 캐럿 위치의 값 기준이 아닌 현재일을 기준으로 날짜가 변경되는 오류를 수정했습니다. |
| 96109 | Calendar 컴포넌트의 type 속성값이 "normal"인 경우 팝업 달력이 표시되면서 표시될 공간 부족으로 일부가 가려지는 오류를 수정했습니다. |

### DataObject

| RP | 설명 |
| --- | --- |
| 95495 | DataObject 오브젝트의 request 메소드 실행 시 onerror 이벤트 핸들러 함수 내에서 ChindFrame 오브젝트의 formurl 속성값을 변경하면 onerror 이벤트가 한번 더 발생하는 오류를 수정했습니다(IE11 브라우저 실행 시 발생한 오류입니다). |

### Dataset

| RP | 설명 |
| --- | --- |
| 95924 | Dataset 오브젝트의 데이터가 null인 상태에서 applyChange 메소드를 실행하고 데이터 입력 후 reset 메소드 실행 시 데이터 값이 null로 돌아가지 않는 오류를 수정했습니다. |

### Edit

| RP | 설명 |
| --- | --- |
| 96274 | Edit 컴포넌트의 autoskip 속성값을 true로 설정한 경우 텍스트 입력 후 Combo 컴포넌트로 포커스 이동 시 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.1200 이후 버전에서 발생한 오류입니다). |
| 96100 | Form에서 띄운 모달창을 닫은 후 Edit 컴포넌트의 캐럿 위치를 마우스로 변경하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1200 이후 버전에서 발생한 오류입니다). |
| 95847 | Edit 컴포넌트의 displaynulltext 속성값을 설정하고 password 속성값을 true로 설정한 후 브라우저의 비밀번호 관리 기능 사용 시 해당 Edit 컴포넌트를 비밀번호 입력 대상으로 인식하지 못하거나 입력한 비밀번호를 그대로 보여주는 오류를 수정했습니다(구글 크롬 브라우저 실행 시 발생한 오류입니다). |

### ExcelExportObject

| RP | 설명 |
| --- | --- |
| 9630696307 | ExcelExportObject 오브젝트의 commdataformat 속성값 변경 후 통신 시 content-type이 속성값에 따라 달라지지 않고 "text/xml"으로만 처리되는 오류를 수정했습니다. |
| 96133 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트의 head 셀을 병합한 경우 병합된 셀 다음 셀이 표시되지 않고 파일이 생성되는 오류를 수정했습니다. |
| 96053 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트에 suppress 속성을 설정하고 Grid 컴포넌트 내 수직 방향 스크롤 이동 후 export 하면 병합된 영역의 border가 표시되지 않고 파일이 생성되는 오류를 수정했습니다. |
| 9604695913 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트에 suppressalign 속성값을 "first"로 설정하고 export 하면 일부 데이터의 정렬 방향이 잘못 처리되어 파일이 생성되는 오류를 수정했습니다. |
| 96007 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트에 cssclass가 적용되는 첫 번째 셀에 빈값이 있는 경우 일부 스타일 속성이 적용되지 못하고 파일이 생성되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 96002 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트의 cssclass 속성값으로 설정한 xcss 파일에서 cell[userstatus=selected]로 설정한 Font 색상이 전체 Row에 반영되어 파일이 생성되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 95827 | ExcelExportObject 오브젝트에서 export 시 대상 Grid 컴포넌트에 병합된 셀이 있는 상태에서 mergeCell 메소드를 실행한 경우 mergeCell 메소드로 병합된 셀이 정상적으로 반영되지 못하고 파일로 생성되는 오류를 수정했습니다. |

### FileDialog

| RP | 설명 |
| --- | --- |
| 96205 | FileDialog 오브젝트 실행 권한 요청이 처리되지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. targetSdkVersion 값을 33으로 설정한 경우 발생한 오류입니다). |

### Form

| RP | 설명 |
| --- | --- |
| 96038 | Form 오브젝트의 onorientationchange 이벤트가 발생하지 않는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |
| 95532 | Form 오브젝트의 stepcount 속성값이 1 이상이고 Div 컴포넌트의 positionstep 속성값이 1 이상인 경우 Div 컴포넌트 내 배치한 Edit 컴포넌트에 포커스가 있는 상태에서 키 입력 시 Form 오브젝트의 stepindex가 0으로 변경되는 오류를 수정했습니다(모바일 WRE 실행 시 발생한 오류입니다. 21.0.0.400 이후 버전에서 발생한 오류입니다). |

### Grid

| RP | 설명 |
| --- | --- |
| 96290 | Grid 컴포넌트 Cell 오브젝트의 combodisplaynulltext 속성값을 설정한 경우 바인딩된 Dataset 오브젝트의 데이터 값이 undefined이면 아무것도 표시하지 않는 오류를 수정했습니다. |
| 96050 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "mask"이고 expr 속성값에 칼럼명을 포함한 수식을 설정하고 text 속성값은 Dataset 오브젝트의 칼럼을 바인딩한 경우 셀 데이터 편집 시도 시 expr 속성값으로 설정한 수식이 반복해서 연산되어 결괏값이 표시되는 오류를 수정했습니다. |
| 96019 | Grid 컴포넌트의 mergeCell 메소드 실행 시 병합된 셀에 selected status가 적용되지 못하는 오류를 수정했습니다. |
| 95875 | Grid 컴포넌트 Cell 오브젝트의 treelevel 속성값에 따라 cssclass가 다르게 적용되도록 설정한 경우 접힌 트리를 펼칠 때 autosizingtype 속성값이 적용되지 못하는 오류를 수정했습니다. |

### ListView

| RP | 설명 |
| --- | --- |
| 96163 | ListView 컴포넌트의 oncellclick 이벤트 핸들러 함수 내에서 showModal 메소드 실행 시 cellbutton이 보이지 않는 오류를 수정했습니다(모바일 WRE 실행 시 발생한 오류입니다). |
| 95814 | XCSS 파일에 설정한 ListView 컴포넌트의 .ListView .body .expandbar[userstatus=collapse] -nexa-icon 이미지가 적용되지 못하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 95778 | ListView 컴포넌트에 바인딩된 Dataset 오브젝트의 filter 메소드를 반복해서 실행 시 filter 기능이 정상 동작하지 못하는 오류를 수정했습니다. |
| 96273 | ListView 컴포넌트의 setBandProperty 메소드 실행 후 showDetailBand 메소드가 실행되지 못하는 오류를 수정했습니다(21.0.0.1300 이후 버전에서 발생한 오류입니다). |

### MaskEdit

| RP | 설명 |
| --- | --- |
| 96204 | MaskEdit 컴포넌트의 텍스트 편집 영역에서 텍스트를 입력하거나 입력된 텍스트를 선택, 삭제할 때 캐럿 위치가 비정상적으로 처리되고 텍스트 선택, 삭제, 입력이 안 되는 오류를 수정했습니다(iOS 운영체제에서 실행 시 발생한 오류입니다). |
| 96203 | MaskEdit 컴포넌트의 type 속성값이 "string"이고 format 속성값이 "####.##.##"인 경우 MaskEdit 컴포넌트 텍스트 편집 영역 터치 시 문자 키패드가 표시되는 오류를 수정했습니다(iOS 운영체제 실행 시 발생한 오류입니다). |

### Sketch

| RP | 설명 |
| --- | --- |
| 96043 | Sketch 컴포넌트의 editmode 속성값이 "text"인 경우 화면을 클릭해서 표시되는 텍스트 입력 상자에 텍스트 입력 후 두 번째 클릭 시 표시되는 텍스트 입력 상자를 한 번 더 클릭해야 캐럿이 표시되고 입력할 수 있는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 96023 | Sketch 컴포넌트의 editmode 속성값이 "text"인 경우 화면을 클릭해서 표시되는 텍스트 입력 상자에 텍스트 입력 후 두 번째 클릭 시 표시되는 텍스트 입력 상자에 텍스트를 입력하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 95912 | Sketch 컴포넌트의 loadSketch 메소드를 실행해 Base64 인코딩 데이터를 화면에 표시한 후 editmode 속성값을 "text"로 변경하고 텍스트 입력 시도 시 텍스트 입력 상자에 잘못된 메시지가 표시되는 오류를 수정했습니다. |
| 95385 | Sketch 컴포넌트의 editmode 속성값을 "stroke"로 설정하고 그림을 그린 후 clear 메소드를 실행하고 Sketch 컴포넌트 외 영역에서 lbuttondown 후 마우스 버튼을 떼지 않고 Sketch 컴포넌트 영역 안으로 이동 시 이전에 그렸던 그림이 다시 표시되는 오류를 수정했습니다. |

### Tab

| RP | 설명 |
| --- | --- |
| 96252 | Tab 컴포넌트 tabbuttonitem 오브젝트의 border-bottom 속성값을 0px로 지정해도 border-bottom가 화면에 표시되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.1300 이후 버전에서 발생한 오류입니다). |

### Textarea

| RP | 설명 |
| --- | --- |
| 96034 | TextArea 컴포넌트의 displaynulltext 속성값을 설정하고 system.print 메소드 실행 시 입력한 값이 있어도 displaynulltext 속성값이 출력되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |

### WebView

| RP | 설명 |
| --- | --- |
| 96010 | 모달창으로 실행한 화면 내 Form 오브젝트의 onload 이벤트 핸들러 함수에서 WebView 컴포넌트의 url 속성값을 설정하는 경우 실행 환경 또는 화면 처리 순서에 따라 간헐적으로 WebView 컴포넌트에 로딩된 콘텐츠가 표시되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다).모달창으로 실행한 화면 내 WebView 컴포넌트의 callScript 메소드 실행 시 실행 환경 또는 화면 처리 순서에 따라 간헐적으로 애플리케이션이 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### 기타 (컴포넌트 / 오브젝트)

| RP | 설명 |
| --- | --- |
| 96316 | open 메소드로 실행한 Form 오브젝트에서 Environment 오브젝트의 datatyperule 속성값이 "1.0"으로만 처리되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 96253 | cssclass 속성값 변경 시 앱이 비정상 종료되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. targetSdkVersion 값을 31 이상으로 설정한 경우 발생한 오류입니다). |
| 96207 | cssclass 속성값 변경 시 background 속성이 반영되지 못하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 96181 | nexacro.open 메소드 실행 시 objParentFrame 파라미터값으로 null 설정 시 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.500 이후 버전에서 발생한 오류입니다). |
| 95858 | loadStyle 메소드 실행 시 모달리스 창에는 반영되지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 95821 | XMLHttpRequest의 onreadystatechange 속성 콜백 함수에서 getAllResponseHeaders 메소드 실행 시 header 값을 반환하지 못하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 9566295232 | 화면 배율 축소 후 모달창을 띄우고 마우스 드래그로 MainFrame 오브젝트 영역 내에서 이동 시 화면 끝까지 이동하지 못하는 오류를 수정했습니다. |
| 95569 | ChildFrame 오브젝트의 move 메소드 실행 후 부모 Frame 오브젝트의 포커스를 다른 곳으로 옮기면 ChildFrame 오브젝트의 위치가 초기화되는 오류를 수정했습니다. |
| 95476 | TCPClientSocket 오브젝트 연결 상태에서 원격 호스트에 의해 강제로 접속이 끊기면서 onerror 이벤트가 발생한 후 open 메소드를 실행해도 재접속되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 9612296041 | setWheelZoom 메소드 실행 시 Form 오브젝트 내 WebBrowser, WebView 컴포넌트에 적용되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### 기타 (transaction)

| RP | 설명 |
| --- | --- |
| 96302 | Dataset 오브젝트의 saveJSON 메소드를 실행하거나 transaction 메소드 실행 시 nDataType 파라미터값을 "3"으로 설정하고 실행할 때 JSON 데이터가 잘못 생성되는 오류를 수정했습니다.- transaction 메소드 실행 시 strInDatasets 파라미터의 추가 옵션 전송 타입이 Normal인 경우 :A 옵션처럼 JSON 데이터가 생성되는 오류- transaction 메소드 실행 시 strInDatasets 파라미터에 2개 이상 Dataset 오브젝트를 설정한 경우 콤마 없이 JSON 데이터가 생성되는 오류- Dataset 오브젝트에 Row가 없거나 Org_ROW에 Column 데이터가 없을 때 JSON 데이터가 잘못 생성되는 오류 |
| 96272 | transaction 메소드 실행 시 수신하는 데이터 형식이 XML이고 2개 이상의 Dataset 데이터를 포함하며 긴 데이터를 포함하거나 Dataset에 값이 없거나 ColumnInfo 정보가 없는 경우 파싱 에러로 데이터를 정상 수신하지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.1300 이후 버전에서 발생한 오류입니다). |
| 96249 | transaction 메소드 실행 시 수신하는 데이터 형식이 SSV이고 Dataset 데이터에 Row 정보가 없는 경우 파싱 에러로 데이터를 정상 수신하지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.500 이후 버전에서 발생한 오류입니다). |
| 96239 | transaction 메소드 실행 시 수신하는 데이터 형식이 XML이고 데이터를 Chunked 방식으로 여러 번에 걸쳐서 받을 경우 파싱 에러로 데이터를 정상 수신하지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.1300 이후 버전에서 발생한 오류입니다). |
| 96176 | transaction 메소드 실행 시 수신하는 데이터 형식이 XML이고 2개 이상의 Dataset 데이터를 포함하며 그중 Row가 없는 Dataset 데이터가 있는 경우 수신된 데이터에 빈 Row가 추가되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.1300 이후 버전에서 발생한 오류입니다). |
| 96099 | transaction 메소드 실행 시 수신하는 데이터 형식이 XML이고 Column 또는 Row 정보가 대용량 데이터인 경우 파싱 에러로 데이터를 정상 수신하지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 21.0.0.700 이후 버전에서 발생한 오류입니다). |
| 96004 | Environment 오브젝트의 httptimeout 속성값을 0으로 설정하고 transaction 메소드 실행 시 간헐적으로 스크립트 에러가 발생하는 오류를 수정했습니다. |

## 16.3.2접근성(Accessibility)

| RP | 설명 |
| --- | --- |
| 96091 | Form 오브젝트의 setWaitCursor 메소드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 95993 | Combo 컴포넌트로 가상 커서 이동 시 displaynulltext 속성값을 두 번 읽어주는 오류를 수정했습니다(안드로이드 운영체제 크롬 브라우저 실행 시 발생한 오류입니다). |
| 9594495680 | Combo 컴포넌트의 type 속성값이 "dropdown"인 경우 Combo 컴포넌트로 가상 커서 이동 후 더블탭 시 combolist가 표시되지 못하는 오류를 수정했습니다(안드로이드 운영체제 크롬 브라우저 실행 시 발생한 오류입니다). |
| 96180 | Form 오브젝트의 stepshowtype 속성값이 "action"인 경우 선택상자 이동 시 stepcontrol에 선택상자가 이동되는 오류를 수정했습니다(안드로이드, iOS 운영체제에서 실행 시 발생한 오류입니다). |

### 센스리더 사용 환경 오류

접근성 관련 오류 중 센스리더 8.1 버전 사용 환경에서 가상 커서를 사용으로 설정하고 구글 크롬 브라우저 실행 시 발생한 오류 처리 건수가 많아 따로 분류해 정리합니다.

| RP | 설명 |
| --- | --- |
| 96271 | 텍스트 편집 영역을 가지고 있는 컴포넌트 앞에 enable 속성값이 false인 Calendar 또는 Spin 컴포넌트가 있는 경우 텍스트 편집 영역을 가지고 있는 컴포넌트로 이동 시 Calendar 또는 Spin 컴포넌트 값을 읽어주는 오류를 수정했습니다. |
| 96270 | stepindex 변경 시 변경되기 전 값을 읽어주는 오류를 수정했습니다. |
| 96228 | showModal 메도스 실행 후 WebBrowser 컴포넌트가 포함된 Modal 창을 닫았을 때 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 96155 | Form 오브젝트에 스크롤이 활성화된 경우 방향키를 사용해 Tab 컴포넌트로 이동하면서 화면 스크롤이 같이 움직이는 경우 Tab 컴포넌트 위치가 아닌 화면 하단으로 이동하는 오류를 수정했습니다. |
| 96128 | Div 컴포넌트의 accessibilityenable 속성값이 false인 경우 가상커서 이동 시 포커스가 이동하지 못하는 오류를 수정했습니다. |
| 96083 | 컴포넌트의 accessibilityenable 속성값이 false인 경우 방향키 입력 시 가상커서가 이동하는 오류를 수정했습니다. |
| 96081 | ImageViewer 컴포넌트의 accessibilityrole 속성값을 "heading"으로 설정하고 ImageViewer 컴포넌트로 가상커서 이동 시 잘못된 값을 읽어주는 오류를 수정했습니다. |
| 96063 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "checkboxcontrol"인 셀을 포함하고 있는 Grid 컴포넌트에서 테이블 모드를 활성화하고 Ctrl + Alt + 방향키를 사용해 셀 이동 시 잘못된 위치로 이동하는 오류를 수정했습니다. |
| 96058 | Tab 키를 사용해 Calendar 컴포넌트로 이동 시 value 값을 두 번 읽어주는 오류를 수정했습니다. |
| 9599295995 | 스크립트에서 컴포넌트의 accessibilitylabel 속성값 변경 시 이전 값을 읽어주는 오류를 수정했습니다. |
| 95989 | 스크립트에서 Grid 컴포넌트의 visible 속성값을 false에서 true로 변경 후 방향키로 Grid 컴포넌트 이동 시 onsetfocus 이벤트가 발생하지 않고 컴포넌트를 벗어날 때 onkillfocus 이벤트가 발생하지 않는 오류를 수정했습니다. |
| 95977 | 컴포넌트의 accessibilityrole 속성값이 "heading"인 경우 가상 커서 이동 시 accessibilityrole 정보를 두 번 읽어주는 오류를 수정했습니다. |
| 95976 | Form 오브젝트에서 위, 아래 방향키로 다른 Form 오브젝트로 이동 시 이전 Form 오브젝트의 titletext 속성값을 읽어주는 오류를 수정했습니다. |
| 96184 | Dataset 오브젝트의 canrowposchange 이벤트 핸들러 함수 내에서 Environment 오브젝트의 enableaccessibility 속성값을 true로 변경하고 return 값을 false로 설정하면 canrowposchange 이벤트가 두 번 발생하는 오류를 수정했습니다(21.0.0.1100 이후 버전에서 발생한 오류입니다). |
| 96159 | Grid 컴포넌트에 가상커서 포커스 이동 시 "빈 줄"이라고 읽는 오류를 수정했습니다. |
| 96081 | ImageViewer 컴포넌트의 accessibilityrole 속성값을 "heading"으로 설정하고 ImageViewer 컴포넌트로 가상커서 이동 시 잘못된 값을 읽어주는 오류를 수정했습니다. |

## 16.3.3QuickCode

| RP | 설명 |
| --- | --- |
| 96255 | 연결된 Action 삭제 후 Controller Chain Editor에서 Trigger를 삭제하지 못하는 오류를 수정했습니다. |
| 96218 | [QuickCode Reference > View Component List] 창에서 View 컴포넌트 삭제 후 다른 View 컴포넌트 선택 시 비정상 종료되는 오류를 수정했습니다. |
| 95958 | Form Template 저장 시 옵션에서 지정한 Form Template 위치가 아닌 기본 경로에 저장되는 오류를 수정했습니다. |

## 16.3.4넥사크로 스튜디오

| RP | 설명 |
| --- | --- |
| 96285 | CheckBox 컴포넌트의 truevalue, falsevalue 속성값 설정 시 속성창 value 속성 항목에 설정한 값이 반영되지 못하는 오류를 수정했습니다. |
| 96267 | 사용자 화면 해상도 설정에 따라 New Layout 창에 표시되는 내용이 잘려서 보이지 않는 오류를 수정했습니다. |
| 95771 | DataObject 오브젝트를 Application Variables에 추가하고 DataObject 오브젝트의 속성값을 설정한 후 DataObject 오브젝트의 이름을 변경하면 속성창에서 설정한 속성값이 초기화된 것으로 표시되는 오류를 수정했습니다. |
| 95240 | 파일명에 공백문자가 포함된 경우 Git Commit이 실패하는 오류를 수정했습니다. |
| 94822 | XCSS 편집 시 predefine으로 등록하지 않은 id를 사용하는 경우 generate 시 predefine 코드가 그대로 노출되고 경고 메시지가 출력되는 오류를 수정했습니다.predefine으로 등록하지 않은 id 사용 시에는 빈 값으로 처리합니다. |
| 94213 | 내 문서 위치가 네트워크 경로인 경우 도움말 실행 시 내용이 표시되지 못하는 오류를 수정했습니다. |
| 9612693059 | 옵션에서 Generate Path 항목에 네트워크 폴더 경로를 지정하지 못하는 오류를 수정했습니다. |

## 16.3.5기타

| RP | 설명 |
| --- | --- |
| 9629696213 | 라이브러리 코드 내 소스 보안 관련 코드를 보완했습니다.- 난수 생성 함수 수정- 빈 Catch 코드 블록 삭제 |
| 96245 | 텍스트가 부드럽게 표시되지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.700 이후 버전에서 발생한 오류입니다). |
| 96200 | 라이브러리 코드 내 Zip Path Traversal 관련 코드를 보완했습니다(안드로이드 운영체제 NRE 실행 시 해당하는 코드 보완입니다).참고: https://support.google.com/faqs/answer/9294009 |
| 96086 | Application 오브젝트의 exit 메소드로 앱 종료 후 앱 재실행 시 비정상 종료되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1100 이후 버전에서 발생한 오류입니다). |

# 16.4도움말 수정 및 추가

| RP | 설명 |
| --- | --- |
| 96173 | 도움말 항목 중 Radio 컴포넌트의 itemaccessibilityenable 속성에 Chrome 지원 여부 표기가 누락된 것을 보완했습니다. |
| 96080 | 도움말 항목 중 Appendix > nexacro_config.xml 항목에 이전 패치에서 작성이 누락된 신규 속성(failpass)을 추가했습니다. |
| 96217 | 도움말 항목 중 ExcelExportObject 오브젝트의 commdataformat 속성 설명 중 기본값 설명이 잘못된 것을 수정했습니다. |

TOBESOFT ©