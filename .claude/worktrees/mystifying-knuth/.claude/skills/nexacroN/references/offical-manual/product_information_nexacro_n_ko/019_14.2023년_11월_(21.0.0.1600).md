---
title: "14.2023년 11월 (21.0.0.1600)"
source: "https://docs.tobesoft.com/product_information_nexacro_n_ko/5a0df50e0fd09968"
crawled_at: "2026-01-09T10:58:15.790Z"
---

# 14.2023년 11월 (21.0.0.1600)

Nexacro N / 모든 매뉴얼에서 검색제품 정보이전 릴리스 노트14.2023년 11월 (21.0.0.1600)article

# 14.2023년 11월 (21.0.0.1600)

## [Download PDF](https://docs.tobesoft.com/r/pdf/download_chapter/fa466c9bb1a71ea9/5a0df50e0fd09968)

playlist\_play

14.1버전정보

14.2릴리스 이후 수정 사항

14.2.121.0.0.1602

14.2.221.0.0.1601

14.3신규기능

14.4변경기능

14.5오류수정

14.5.1기본 컴포넌트 / 오브젝트

Calendar

CheckBoxSet

DataObject

Dataset

Div

Edit

ExcelImportObject/ExcelExportObject

FileDialog

FileUpTransfer/FileDownTransfer

Grid

ImageViewer

ListBox

MaskEdit

MultiCombo

PopupMenu

Sketch

Tab

TextArea

VirtualFile

WebView

XPush

기타 (컴포넌트 / 오브젝트)

기타 (transaction)

14.5.2DeviceAPI

14.5.3접근성(Accessibility)

센스리더 사용 환경 오류

14.5.4Fluid Layout

14.5.5넥사크로 스튜디오

14.5.6넥사크로 모듈 디벨로퍼

14.5.7기타

14.6도움말 수정 및 추가

# 14.1버전정보

| Product Version | 21.0.0.1600 |
| --- | --- |
| File Version | 2023.12.5.1 |

- 사용 환경이나 브라우저 기능 차이 또는 제품 제약으로 발생할 수 있는 현상은 제품제약사항을 참고해주세요.
 
- 새로운 기능, 변경된 기능 목록에서 버전별 새로운 기능, 변경된 기능 목록을 확인할 수 있습니다.

# 14.2릴리스 이후 수정 사항

## 14.2.121.0.0.1602

| RP | 설명 |
| --- | --- |
| 9853198530 | Div 컴포넌트 내에 배치된 TextArea 컴포넌트의 scrolltype 속성값이 "none"이고 right 속성값이 0인 경우 Form 오브젝트의 크기가 변경되거나 줌 동작 시 스크롤이 활성화되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 98564 | Static 컴포넌트의 usedecorate 속성값이 true인 경우 태그 사이에 공백문자가 2개 이상 포함되는 경우 태그가 적용되지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |

## 14.2.221.0.0.1601

| RP | 설명 |
| --- | --- |
| 98707 | 도움말 변경 사항이 누락된 것을 보완했습니다.system.checkDevicePermissionsystem.requestDevicePermission신규(RP 97992) 관련 도움말ExportItem > valuetypebandidExcelExportObject > addExportItem |

# 14.3신규기능

1. **\[접근성\] ListView 컴포넌트에 접근성 기능이 동작하도록 기능을 추가했습니다. (RP 98425, 97313)**
 

| 설명 |
| --- |
| ListView 컴포넌트에서 접근성 기능을 지원합니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > ListViewAccessibility Key Action을 확인하시기 바랍니다. |

2. **Windows NRE 환경에서 ImageViewer 컴포넌트에 SVG Data Uri Scheme을 지원하도록 기능을 추가했습니다. (RP 97090)**
 

| 설명 |
| --- |
| Windows NRE 환경에서 ImageViewer 컴포넌트에 SVG Data Uri Scheme을 지원합니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > ImageViewer > Property > image |

3. **\[접근성\] 접근성 환경에서 Grid, ListView 컴포넌트에 노출되지 않은 Row를 읽을 수 있도록 기능을 추가했습니다. (RP 98199, 98522)**
 

| 설명 |
| --- |
| createrowstype Property를 추가했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > Grid > Property > createrowstypeComponents > Component > ListView > Property > createrowstype |

4. **Grid Cell에 이미지 로드를 실패했을 경우 이벤트가 발생하도록 기능을 추가했습니다. (RP 98198)**
 

| 설명 |
| --- |
| Grid의 onimageerror Event를 추가했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > Grid > Event > onimageerror |

5. **Windows NRE 환경에서 networksecurelevel = all 인 경우 WebView에 Crossdomain을 사용 가능하도록 기능을 추가했습니다. (RP 98281, 97974)**
 

| 설명 |
| --- |
| set_url이 CrossDomain 상황이면 "안전하지 않은 도메인에 접근합니다. 계속 진행하시겠습니까?" Confirm 창을 발생시킵니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > WebView > Method > callScript |

6. **WAF 환경에서 Export/Import를 사용할 수 있도록 기능을 추가했습니다. (RP 98027, 97442)**
 

| 설명 |
| --- |
| Export/Import에 commandversion 기능을 추가했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Misc. Objects > ExcelExportObject > Property > commandversionMisc. Objects > ExcelImportObject > Property > commandversion |

7. **Export 시 Grid의 Band를 구분할 수 있도록 기능을 추가했습니다. (RP 97992)**
 

| 설명 |
| --- |
| addExportItem 전달 인자를 추가했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Misc. Objects > ExcelExportObject > Method > addExportItem |

8. **CheckBoxSet 컴포넌트에 textseparator 속성을 추가했습니다 (RP 98489, 97289)**
 

| 설명 |
| --- |
| 2개 이상의 text 속성값 설정 시 구분자로 사용할 값을 설정하는 textseparator 속성을 추가했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > CheckBoxSet > Property > textseparator |

# 14.4변경기능

1. **MultiCombo edittype의 기능 일부가 변경됐습니다. (RP 97644, 97643, 97642)**
 

| 설명 |
| --- | --- |
| 변경 전 | edittype = single/multitagedittype = text/count |
| 변경 후 | edittype = single/multitagdropbutton 추가tag delete키 삭제 기능 추가edittype = text/countpopuplist에 edit추가 (입력받는 영역) |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > MultiComboStructure, Contents Sizing, Basic Key Action을 확인하시기 바랍니다. | |

2. **Android NRE API Level을 변경했습니다. (RP 95543)**
 

| 설명 |
| --- | --- |
| 변경 전 | Android API Level 30 |
| 변경 후 | Android API Level 33 |
| 해당 버전 이상 대상 프로젝트에서는 NOTIFICATIONS 권한을 설정해주어야 합니다.자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Misc. Objects > System > Method > requestDevicePermissionMisc. Objects > System > Method > checkDevicePermission | |

3. **usedecorate 속성 적용 시 사용할 수 있는 형식에 제한 사항을 추가했습니다. (RP 97671)**
 

| 설명 |
| --- | --- |
| 변경 전 | 지정된 Type 태그 이외의 값을 허용 |
| 변경 후 | 지정된 Type 태그 이외의 값을 입력하는 경우에는 삭제 처리 |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > Component > Static > Property > usedecorateComponents > Component > Grid > Objects > GridCellControl > Property > displaytypeComponents > Component > Grid > Objects > GridCellControl > Objects > CellSubCellControl > Property > displaytypeComponents > Component > ListView > Objects > ListViewCellControl > Property > displaytype | |

4. **onbeforeclose, onclose 이벤트가 상위 오브젝트로 전파되도록 기능을 수정했습니다. (RP 97440, 97493)**
 

| 설명 |
| --- |
| reload, close 사용 시 onbeforeclose, onclose 이벤트가 상위 오브젝트로 전파되지 않는 문제를 수정했습니다.이벤트 버블이 변경됐으므로 기능 사용에 확인이 필요합니다. |

5. **Grid 컴포넌트의 트리 확장/축소(treeuseexpandkey 속성값이 "false" 일 때) Key Action을 변경했습니다. (RP 95650)**
 

| 설명 |
| --- | --- |
| 변경 전 | Alt + 좌우방향키 또는 Option + 좌우방향키 |
| 변경 후 | Ctrl + Alt + 좌우방향키 또는 Ctrl + Option + 좌우방향키 |

# 14.5오류수정

## 14.5.1기본 컴포넌트 / 오브젝트

### Calendar

| RP | 설명 |
| --- | --- |
| 98255 | Calendar 컴포넌트의 popuptype 속성값이 "system"인 경우 컴포넌트로 포커스 이동 후 터치 시 비정상 종료되는 오류를 수정했습니다(iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 97747 | Calendar 컴포넌트의 value 값을 설정한 상태에서 실행 후 텍스트 편집 영역에서 값을 지우고 연도만 입력하고 Enter 키 입력 시 날짜가 11월 30일로 표시되는 오류를 수정했습니다. |
| 97521 | Calendar 컴포넌트의 autoselect 속성값이 true이고 value 속성값을 설정하지 않은 경우 getNextComponent 메서드로 포커스 이동 시 autoselect 동작을 하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97477 | Form 오브젝트의 getNextComponent 메서드를 실행해 Calendar 컴포넌트로 포커스가 옮겨진 경우 autoselect 속성이 적용되지 못하는 오류를 수정했습니다. |
| 97407 | Calendar 컴포넌트의 type 속성값이 "monthonly"이고 usetrailingday 속성값이 true인 경우 trailingday 영역에 표시되는 날짜 선택 시 다음 달 달력으로 넘어가면서 선택했던 위치에 seleted 스타일이 남아있는 오류를 수정했습니다. |
| 97389 | Calendar 컴포넌트의 datepickerchangetype 속성값이 "button"일 경우 연도 변경 시 선택 상태가 잘못 표시되는 오류를 수정했습니다. |

### CheckBoxSet

| RP | 설명 |
| --- | --- |
| 98468 | Environment enabletouchevent 속성값이 true인 경우 CheckBoxSet 컴포넌트에서 항목 선택 시 이전에 선택한 항목이 선택 해제되는 오류를 수정했습니다(모바일 NRE, WRE 실행 시 발생한 오류입니다). |
| 97402 | CheckBoxSet 컴포넌트에서 2개 이상의 값을 선택했을 때 index 속성값이 잘못된 값으로 처리되는 오류를 수정했습니다. |
| 97387 | CheckBoxSet 컴포넌트의 value 속성값을 Dataset 오브젝트와 바인딩한 경우 readonlycolumn 값이 true인 아이템에 값이 적용되지 못하는 오류를 수정했습니다. |

### DataObject

| RP | 설명 |
| --- | --- |
| 97599 | DataObject 오브젝트의 request 메서드 실행 시 Environment의 onerror 이벤트 핸들러 함수 내에서 true 값을 반환해도 DataObject 오브젝트의 onerror 이벤트가 발생하는 오류를 수정했습니다. |

### Dataset

| RP | 설명 |
| --- | --- |
| 97841 | Dataset 오브젝트의 Column type 속성값을 "BIGDECIMAL"로 변경하고 keystring 속성을 설정해 데이터 정렬 시 변경한 Column type 속성에 따라 정렬되지 못하는 오류를 수정했습니다. |
| 97217 | Dataset 오브젝트의 Const Column 값이 빈 문자열("")인 경우 setConstColumn 메서드 실행해 0으로 변경하려고 시도하면 값이 변경되지 못하는 오류를 수정했습니다. |

### Div

| RP | 설명 |
| --- | --- |
| 97575 | Div 컴포넌트의 async 속성값에 따라 TabpageControl 오브젝트의 연결된 Form의 레이아웃 선택 동작이 달라지는 오류를 수정했습니다. |

### Edit

| RP | 설명 |
| --- | --- |
| 98566 | Edit 컴포넌트의 inputtype 속성값이 "digit"이고 maxlength 속성값을 설정한 경우 숫자값을 복사해 붙여 넣을 때 마지막 숫자가 잘려서 입력되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97606 | Edit 컴포넌트의 taborder 속성값이 0인 경우 입력된 문자열 중간에 마우스 클릭 후 글자 입력 시 문자열 마지막에 글자가 입력되는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97556 | Edit 컴포넌트의 readonly 속성값이 true이고 maxlength 속성값을 설정한 경우 Edit 컴포넌트 클릭 시 maxlength 속성값으로 설정한 길이만큼 value 속성값으로 설정한 문자열이 잘려서 표시되는 오류를 수정했습니다. |
| 97382 | Edit 컴포넌트의 inputtype 속성값을 "english,number"로 설정한 경우 키보드의 숫자 키패드로 숫자를 입력하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 한글 세벌식 키보드 사용 시 발생한 오류입니다). |
| 97315 | Form 오브젝트 또는 Div 컴포넌트 스크롤 이동 시 Edit 컴포넌트의 커서가 컴포넌트 영역을 벗어나는 오류를 수정했습니다(iOS 운영체제에서 Chrome 브라우저 실행 시 발생한 오류입니다). |

### ExcelImportObject/ExcelExportObject

| RP | 설명 |
| --- | --- |
| 98572 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "mask"이고 ExcelExportObject 오브젝트의 transferrowcount 속성값을 설정한 경우 exportData 메서드 실행 시 transferrowcount 속성값으로 설정한 Row 이후의 데이터가 처리되지 못하는 오류를 수정했습니다. |
| 98497 | ExcelExportObject 오브젝트의 exportData 메서드 실행 시 파일명에 "%" 문자가 포함된 경우 잘못된 다운로드 경로로 연결되는 오류를 수정했습니다. |
| 98354 | ExcelImportObject 오브젝트의 importData 메서드의 strSource 파라미터값을 변경하지 않고 메서드를 반복적으로 실행했을 때 정상적으로 처리했음에도 onerror 이벤트가 발생하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 97792 | ExcelExportObject 오브젝트의 commdataformat 속성값을 "json"으로 설정하고 넥사크로 스튜디오에서 Environment의 Cookie 값을 설정한 경우 exportData 메서드 실행 시 Parameters 데이터 내 구분자(,)가 누락되는 오류를 수정했습니다. |

### FileDialog

| RP | 설명 |
| --- | --- |
| 96937 | FileDialog 컴포넌트의 accept 속성값을 ".png"로 설정한 경우 앱이 비정상 종료되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |

### FileUpTransfer/FileDownTransfer

| RP | 설명 |
| --- | --- |
| 98389 | FileDownTransfer 컴포넌트의 download 메서드 실행 시 다운로드하려는 파일과 같은 파일이 열려 있는 경우 onerror 이벤트가 발생하지 않는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 97640 | FileUpTransfer 오브젝트의 upload 메서드 실행 시 파일을 업로드하지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1100 이후 버전에서 발생한 오류입니다). |

### Grid

| RP | 설명 |
| --- | --- |
| 98034 | Grid 컴포넌트의 cellsizingtype 속성값을 "col"로 설정한 경우 마우스 드래그 동작으로 Column의 너비를 0으로 조절한 이후 다시 다른 값으로 변경하기 어려운 기능을 개선했습니다(마우스 드래그 동작 범위를 조금 더 넓게 설정했습니다). |
| 97940 | Grid 컴포넌트의 autosizingtype 속성값을 "none"이 아닌 값으로 설정하고 displaytype 속성값이 "checkboxcontrol"인 셀을 클릭하면 스크립트 에러가 발생하는 오류를 수정했습니다(모바일 WRE 실행 시 발생한 오류입니다). |
| 97882 | Grid 컴포넌트의 autoenter 속성값이 "select"인 경우 바인딩된 Dataset 오브젝트의 oncolumnchanged 이벤트 핸들러 함수 내에서 moveToNextCell 메서드를 실행해 다음 셀로 포커스가 이동했을 때 이전 셀의 편집 상태가 닫히지 않는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97793 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "mask"이고 maskeditautoselect 속성값이 true인 경우 셀 영역 터치 시 텍스트를 전체선택이 취소되고 키패드가 사라지는 오류를 수정했습니다(안드로이드 운영체제 WRE 실행 시 발생한 오류입니다). |
| 97768 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "combocontrol"인 Row를 선택한 상태에서 마우스 휠을 돌리면 onmousewheel 이벤트가 발생하지 못하는 오류를 수정했습니다. |
| 97765 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "mask" 또는 "number"인 경우 터치 동작 시 키패드가 내려갔다가 올라오는 오류를 수정했습니다(안드로이드 운영체제에서 삼성 브라우저 사용 시 발생한 오류입니다). |
| 97764 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "mask" 또는 "number"인 경우 터치 동작 시 커서가 브라우저 상단에서 편집영역으로 움직이는 오류를 수정했습니다(iOS 16 이상 버전에서 발생한 오류입니다. Dynamic Island가 실행된 경우 발생한 오류입니다). |
| 97669 | Grid 컴포넌트의 셀 데이터 편집 시 바인딩된 Dataset 오브젝트의 cancolumnchange 이벤트 핸들러 함수 내에서 showModal 메서드를 실행하는 경우 Grid 컴포넌트의 셀의 편집 상태가 닫히지 않는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97647 | Grid 컴포넌트의 enable 속성값에 따라 cellexpandbutton의 상태가 바뀌지 못하는 오류를 수정했습니다. |
| 97628 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "multicombo"인 경우 해당 셀 더블 클릭 시 첫 번째 값이 사라지는 오류를 수정했습니다. |
| 97605 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "textarea"인 경우 화면 로딩 후 바로 해당 셀에 입력을 시도하면 포커스가 사라지고 입력하지 못하는 오류를 수정했습니다(iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 97543 | Screen userzoom 속성값을 true로 설정한 경우 Grid 컴포넌트의 스크롤 동작이 정상적으로 실행되지 못하는 오류를 수정했습니다(iPad 운영체제에서 Safari 브라우저를 데스크톱 모드로 실행한 경우 발생한 오류입니다). |
| 97378 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "multicombo", multicomboedittype 속성값이 "singletag" 또는 "multitag"이고 스크롤바가 표시된 경우 스크롤바 영역 클릭 시 포커스가 이동하지 않는 오류를 수정했습니다. |
| 97308 | Grid 컴포넌트 Cell 오브젝트의 calendardisplaynulltype 속성값을 "none"이 아닌 값으로 설정하고 해당 셀의 값을 빈 문자열("")로 설정한 경우 null처럼 처리되는 오류를 수정했습니다. |
| 97210 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "combocontrol"이고 edittype 속성값이 "combo"인 경우 해당 셀 값을 변경했을 때 변경된 값이 반영되지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97134 | Grid 컴포넌트 내 head, summary band의 스크롤 영역 갱신이 되지 않아 화면 해상도에 따라 head, summay band의 위치가 잘못 표시되는 오류를 수정했습니다. |
| 96987 | Grid 컴포넌트의 scrollbartype 속성값이 "none none"인 경우 셀 클릭 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 96923 | Grid 컴포넌트의 right 속성값을 설정하고 마우스 휠을 돌려 화면을 축소한 경우 onsize 이벤트 핸들러 함수 내에서 getRealRowFullSize 메서드 실행 시 실제 Row 높이와 차이가 발생하는 오류를 수정했습니다. |
| 96713 | Grid 컴포넌트의 autosizingtype 속성값이 "row"인 경우 Cell 오브젝트의 displaytype 속성값을 expr로 설정하면 "checkboxcontrol" 또는 "radioteimcontrol"일 때 크기가 잘못 계산되는 오류를 수정했습니다. |
| 96644 | Grid 컴포넌트의 nodataimage 속성값을 설정하고 바인딩된 Dataset 오브젝트의 appendData 메서드를 실행해 데이터를 추가하는 경우 nodataimage 속성으로 설정한 이미지 잔상이 표시되는 오류를 수정했습니다. |
| 95966 | Grid 컴포넌트의 scrolldisplaymode 속성값을 "line"으로 설정한 경우 바인딩된 Dataset 오브젝트의 copyData 메서드 실행 후 Grid 컴포넌트 스크롤 동작 시 스크립트 에러가 발생하는 오류를 수정했습니다. |

### ImageViewer

| RP | 설명 |
| --- | --- |
| 98011 | ImageViewer 컴포넌트의 image 속성값으로 Base64 인코딩 데이터 설정 시 메모리 누수가 발생하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |

### ListBox

| RP | 설명 |
| --- | --- |
| 98502 | ListBox 컴포넌트에 바인딩된 Dataset 오브젝트의 deleteMultiRows 메서드 실행 시 삭제하지 않은 항목이 표시되지 못하는 오류를 수정했습니다. |
| 96565 | ListBox 컴포넌트에서 바인딩된 Dataset 오브젝트의 addRow 메서드를 실행하고 아이템 목록 스크롤바를 맨 아래로 이동하는 동작을 반복했을 때 값이 없는 Row가 추가되는 오류를 수정했습니다. |

### MaskEdit

| RP | 설명 |
| --- | --- |
| 96975 | MaskEdit 컴포넌트의 -nexa-padding 속성값에 따라 -nexa-text-align 속성값을 "center"로 설정한 경우 텍스트 정렬이 적용되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 95149 | MaskEdit 컴포넌트의 enable 속성값을 false로 설정하고 color 속성값 변경 시 반영되지 못하는 오류를 수정했습니다. |

### MultiCombo

| RP | 설명 |
| --- | --- |
| 98473 | MultiCombo 컴포넌트의 type 속성값이 "dropdown" 이외의 값으로 설정한 경우 텍스트 편집 영역에 값을 입력하고 포커스를 다른 컴포넌트로 이동했다가 다시 MultiCombo 컴포넌트를 선택했을 때 이전에 입력한 값이 남아 있는 오류를 수정했습니다. |
| 98472 | MultiCombo 컴포넌트의 type 속성값이 "dropdown" 이외의 값으로 설정한 경우 텍스트 편집 영역 클릭 시 입력 커서가 표시되지 않은 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 97402 | MultiCombo 컴포넌트에서 2개 이상의 값을 선택했을 때 index 속성값이 잘못된 값으로 처리되는 오류를 수정했습니다. |
| 97395 | MultiCombo 컴포넌트의 edittype 속성값이 "text"인 경우 바인딩된 Dataset 오브젝트의 값이 변경되어도 반영되지 못하는 오류를 수정했습니다. |
| 97394 | MultiCombo 컴포넌트의 edittype 속성값이 "text"이고 type 속성값이 "search"인 경우 입력하는 문자열이 아이템 목록에 배치된 순서에 따라 검색되지 않는 오류를 수정했습니다. |
| 97393 | MultiCombo 컴포넌트의 type 속성값을 "dropdown" 이외의 값으로 설정하고 아이템 목록에 없는 값을 입력한 이후 아이템 목록에 있는 값을 입력해도 텍스트 편집 영역이 nulltext 상태로 표시되는 오류를 수정했습니다. |
| 97387 | MultiCombo 컴포넌트의 value 속성값을 Dataset 오브젝트와 바인딩한 경우 readonlycolumn 값이 true인 아이템에 값이 적용되지 못하는 오류를 수정했습니다. |
| 97377 | MultiCombo 컴포넌트의 type 속성값이 "filter"인 경우 Enter 키로 아이템 선택이 안 되는 오류를 수정했습니다. |
| 97376 | MultiCombo 컴포넌트의 type 속성값이 "filter", "filterlike", "caseifilter", "caseifilterlike" 중 하나이고 showselectallcheckbox 속성값이 true인 경우 필터 적용 후 아이템 전체 선택 후 multicombolist를 닫았다가 다시 펼치면 선택이 안된 상태로 표시되는 오류를 수정했습니다. |
| 97375 | MultiCombo 컴포넌트의 showselectallcheckbox 속성값이 true인 경우 selectallcheckbox의 폰트 크기가 커지는 경우 multicombolist에서 잘려 표시되는 오류를 수정했습니다. |
| 97363 | MultiCombo 컴포넌트의 type 속성값을 "dropdown" 이외의 값으로 설정하고 edittype 속성값을 "count"로 설정한 경우 전체 아이템 개수가 실제 아이템 목록 개수와 다르게 표시되는 오류를 수정했습니다. |
| 97358 | MultiCombo 컴포넌트의 displaynulltext 속성값 설정 시 선택한 아이템이 있어도 displaynulltext가 표시되거나 displaynulltext 일부가 잘려 표시되는 오류를 수정했습니다. |

### PopupMenu

| RP | 설명 |
| --- | --- |
| 98091 | PopupMenu 컴포넌트에 표시되는 메뉴 아이템 개수가 많은 경우 prevbutton, nextbutton 컨트롤이 표시되지 않고 데이터가 잘려서 표시되는 오류를 수정했습니다. |

### Sketch

| RP | 설명 |
| --- | --- |
| 97443 | Sketch 컴포넌트의 clear 메서드 실행 후 컴포넌트 영역 터치 시 이전에 그린 이미지가 표시되는 오류를 수정했습니다(화면 터치를 지원하는 기기에서 실행 시 발생한 오류입니다). |

### Tab

| RP | 설명 |
| --- | --- |
| 97773 | TabpageControl 오브젝트의 연결된 Form의 레이아웃 변경 시 onlayoutchanged 이벤트가 2번 발생하는 오류를 수정했습니다. |
| 97534 | Tab 컴포넌트의 removeTabpage 메서드 실행 시 onbeforeclose, onclose 이벤트가 2번 발생하는 오류를 수정했습니다. |

### TextArea

| RP | 설명 |
| --- | --- |
| 98667 | TextArea 컴포넌트의 scrolltype 속성값이 "none"일 경우 마우스 휠을 돌리면 스크롤이 이동하는 오류를 수정했습니다(Firefox 브라우저 실행 시 발생한 오류입니다). |

### VirtualFile

| RP | 설명 |
| --- | --- |
| 98283 | VirtualFile 오브젝트에서 파일 크기가 200MB 이상인 파일 처리 시 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### WebView

| RP | 설명 |
| --- | --- |
| 97461 | WebView 컴포넌트의 url 속성값을 설정하지 않은 상태에서 callScript 메서드의 파라미터로 document.write 구문을 실행 시 화면이 로드되지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 실행 시 발생한 오류입니다). |
| 97184 | WebView 컴포넌트가 포함된 Form 오브젝트에서 system.showModalSync 메서드 실행 시 WebView 컴포넌트 영역이 화면에 표시되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### XPush

| RP | 설명 |
| --- | --- |
| 96572 | XPush 오브젝트의 subscribe 메서드 실행 시 strType 파라미터 값을 "update"로 설정한 경우 수신된 메시지가 업데이트되지 못하는 오류를 수정했습니다(iOS 운영체제 NRE 실행 시 발생한 오류입니다). |
| 96571 | XPush 오브젝트의 unsubscribe 메서드 실행 시 onerror 이벤트가 발생하고 데이터가 계속 수신되는 오류를 수정했습니다(iOS 운영체제 NRE 실행 시 발생한 오류입니다). |

### 기타 (컴포넌트 / 오브젝트)

| RP | 설명 |
| --- | --- |
| 98553 | Div 컴포넌트에 WebBrowser 컴포넌트와 Edit 컴포넌트가 포함된 Form 오브젝트를 url 속성값으로 설정하고 로딩하는 경우 Edit 컴포넌트 값 편집 시 키를 입력할 때마다 바인딩된 Dataset 오브젝트의 cancolumnchange 이벤트가 발생하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97829 | 컴포넌트의 bottom 속성값을 설정한 경우 최초 로딩 시 컴포넌트가 표시되지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97667 | Form 오브젝트의 layoutchanged 이벤트 핸들러 함수 내에서 Div 컴포넌트 내 배치된 컴포넌트 크기를 변경하지 못하는 오류를 수정했습니다. |
| 97481 | Form 오브젝트 내 Tab 컴포넌트가 배치된 경우 Form 또는 상위 Frame 오브젝트의 url 속성값 변경 시 onbeforeclose, onclose 이벤트가 2번 발생하는 오류를 수정했습니다. |
| 97287 | 컴포넌트 영역에서 더블탭 동작 시 컴포넌트가 확대되는 오류를 수정했습니다(iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 97070 | Combo 컴포넌트의 onitemchanged 이벤트 핸들러 함수 내에서 setFocus 메서드를 사용해 Edit 컴포넌트로 포커스를 이동했을 때 Edit 컴포넌트에 커서가 보이지 않고 키패드도 표시되지 않는 오류를 수정했습니다(안드로이드 운영체제 13 버전에서 WRE 실행 시 발생한 오류입니다). |
| 96553 | Div, PopupDiv, View, TabpageControl에서 formscrolltype, formscrollbartype 속성값 설정 시 연결된 Form 오브젝트의 scrolltype, scrollbartype 속성값이 변경되는 오류를 수정했습니다.Div, PopupDiv, View, TabpageControl에 연결된 Form 오브젝트 변경 시 formscrolltype, formscrollbartype 속성값이 변경되는 오류를 수정했습니다.Div 컴포넌트의 formscrolltype 속성값이 "none"인 경우 Tab키 입력 시 컴포넌트로 포커스가 이동하지 못하는 오류를 수정했습니다. |
| 96407 | 웹브라우저 화면 확대 후 showModal 메서드 실행 시 마우스 드래그 동작으로 모달창 타이틀바 영역을 선택해서 이동하지 못하는 오류를 수정했습니다(Desktop WRE 실행 시 발생한 오류입니다). |

### 기타 (transaction)

| RP | 설명 |
| --- | --- |
| 98463 | transaction 메서드 실행 시 통신 에러가 발생한 경우 데이터가 업데이트되는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다. 21.0.0.500 이후 버전에서 발생한 오류입니다). |
| 97932 | transaction 메서드를 반복해서 실행하는 경우 메모리가 누적되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97848 | transaction 메서드 실행 시 nDataType 파라미터 값을 "3"으로 설정하고 ConstColumn의 value 값에 큰 따옴표(")가 포함된 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 97621 | Edit 컴포넌트의 onkeyup 이벤트 핸들러 함수 내에서 transaction 메서드 시행 시 ESC 키 입력으로 transaction이 중단되지 못하는 오류를 수정했습니다. |
| 96401 | transaction 메서드 실행 시 ColumnInfo, Row 정보가 없거나 ColumnInfo가 나누어 전송되는 경우 데이터가 잘못 처리되는 오류를 수정했습니다. |

## 14.5.2DeviceAPI

| RP | 설명 |
| --- | --- |
| 97762 | ImagePicker 오브젝트의 open 메서드가 실행되지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. targetsdkversion을 "33"으로 설정 시 발생한 오류입니다). |
| 97723 | AudioRecorder 오브젝트의 recordingStop 메서드 실행 시 마이크가 꺼지지 않는 오류를 수정했습니다(iOS 운영체제 NRE 실행 시 발생한 오류입니다). |

## 14.5.3접근성(Accessibility)

| RP | 설명 |
| --- | --- |
| 98461 | Combo 컴포넌트의 innerdataset의 datacolumn 값이 "all"인 경우 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 98313 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "displaytype"인 셀 접근 시 "빈줄"이라고 여러 번 읽는 오류를 수정했습니다(크롬 브라우저 실행 시 발생한 오류입니다). |
| 97552 | TextArea 컴포넌트 클릭 시 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 96911 | TabpageControl 오브젝트로 포커스 이동 시 스크롤이 움직이는 오류를 수정했습니다. |
| 96882 | 가상 커서를 해제한 상태에서 키액션이 동작하지 못하는 오류를 수정했습니다. |
| 96871 | MaskEdit 컴포넌트에 포커스 이동 시 value 속성값을 읽지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 96866 | Button 컴포넌트를 동적으로 생성하고 cssclass 속성으로 -nexa-icon 속성값을 설정한 경우 가상커서 이동 시 이미지 정보를 두 번 읽는 오류를 수정했습니다. |
| 96824 | Calendar 컴포넌트의 type 속성값이 "monthonly"일 경우 날짜 정보를 2번 읽는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 96803 | 방향키 입력으로 WebView 컴포넌트 접근 시 WebView 컴포넌트에서 로딩한 콘텐츠로 이동하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 96021 | Grid 컴포넌트 내 편집할 수 있는 셀에 데이터 입력 후 Enter 키 입력 시 포커스가 다른 컴포넌트로 이동하는 오류를 수정했습니다. |

### 센스리더 사용 환경 오류

접근성 관련 오류 중 센스리더 8.2, 8.3 버전 사용 환경에서 가상 커서를 사용으로 설정하고 구글 크롬 브라우저 실행 시 발생한 오류 처리 건수가 많아 따로 분류해 정리합니다.

센스리더 사용 환경이 다른 경우에는 실행 환경을 별도 기재했습니다.

| RP | 설명 |
| --- | --- |
| 98595 | Tab 컴포넌트로 포커스 이동 시 스크롤이 움직이는 오류를 수정했습니다. |
| 98517 | 상하 방향키로 Static 컴포넌트로 이동 시 "빈줄"이라고 읽는 오류를 수정했습니다. |
| 98395 | 상하 방향키 입력 시 Frame 오브젝트로 포커스가 이동하는 오류를 수정했습니다. |
| 98336 | Button 컴포넌트에서 Enter 키 입력 후 Grid 컴포넌트로 이동해서 편집할 수 있는 셀 영역 접근 시 편집 상태로 전환되지 못하는 오류를 수정했습니다. |
| 97801 | Grid 컴포넌트에 바인딩된 Dataset 오브젝트의 데이터가 없는 경우 상하방향키로 이동 시 음성 출력이 되지 않는 오류를 수정했습니다. |
| 97237 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "date"인 셀로 이동 시 음성 출력을 하지 못하는 오류를 수정했습니다. |
| 96993 | 키보드로 Grid 컴포넌트의 셀 이동 시 마우스로 다른 위치의 편집할 수 있는 셀을 클릭하면 편집 상태로 전환되지 못하는 오류를 수정했습니다. |
| 96900 | Div 컴포넌트로 포커스 이동을 하지 못하는 오류를 수정했습니다. |
| 96865 | Grid 컴포넌트 다음에 Button 컴포넌트가 위치한 경우 Button 컴포넌트로 가상 커서 이동 시 onclick 이벤트가 발생하는 오류를 수정했습니다. |
| 96864 | Grid 컴포넌트의 nodatatext 속성값을 동적으로 변경하면 Body band 정보를 읽지 못하는 오류를 수정했습니다. |
| 96020 | Tab 키 입력으로 Grid 컴포넌트에 이동 시 음성 출력이 되지 않는 오류를 수정했습니다. |

## 14.5.4Fluid Layout

| RP | 설명 |
| --- | --- |
| 98246 | type 속성값이 "table"인 경우 배치된 컴포넌트의 tablecellarea 속성값이 특정값일 때 컴포넌트가 사라져 보이는 오류를 수정했습니다. |
| 97860 | type 속성값이 "default"로 설정된 화면 로딩 시 onlayoutchanged 이벤트가 발생하는 오류를 수정했습니다. |

## 14.5.5넥사크로 스튜디오

| RP | 설명 |
| --- | --- |
| 98332 | Outlineview 패널이 열려있지 않은 상태에서 Form 로딩 속도가 느려지는 오류를 수정했습니다. |
| 97999 | 디자인 화면에서 Div 컴포넌트 내에 Div 컴포넌트를 배치하고 내부에 있는 Div 컴포넌트를 선택한 상태에서 컨텍스트 메뉴를 실행하고 [Delete] 항목 선택 시 넥사크로 스튜디오가 비정상 종료되는 오류를 수정했습니다. |
| 97956 | CheckBox 컴포넌트의 truevalue 또는 falsevalue 속성값을 설정한 경우 다른 컴포넌트를 선택했다가 다시 CheckBox 컴포넌트를 선택했을 때 디자인 화면에 표시되는 상태와 value 속성값이 다르게 표시되는 오류를 수정했습니다. |
| 97689 | 속성창에서 컴포넌트의 locale 속성값 선택 시 목록에 "en_NL" 항목이 표시되지 못하는 오류를 수정했습니다. |
| 97571 | 디버깅 동작 시 break 포인트를 설정한 위치와 디버깅 시 멈추는 위치가 다르게 처리되는 오류를 수정했습니다. |
| 97508 | Div 컴포넌트의 position 관련 속성값의 단위를 "px"에서 "%"로 변경 후 Div 컴포넌트 내 배치된 컴포넌트의 left 속성값을 속성값에서 지우면 right 속성값이 설정되면서 값이 잘못 계산되는 오류를 수정했습니다. |
| 97482 | 프로젝트 Generate 경로에 HTML 파일을 가져다 놓고 Local Web Server로 실행한 경우 WebView 컴포넌트에서 HTML 파일을 로딩한 후 callScript 메서드를 실행하면 Return 값이 없는 오류를 수정했습니다. |
| 97456 | XCSS 편집기에서 GridCellControl, CellSubCellControl, ListViewCellControl 선택자 하위 선택자 목록에 cellmulticombo가 표시되지 못하는 오류를 수정했습니다. |
| 97455 | Source 탭에서 default 레이아웃이 아닌 추가 레이아웃에 이벤트를 설정한 경우 잘못된 형태로 generate 되는 오류를 수정했습니다. |
| 97438 | Grid Contents Editor에서 Cell 오브젝트의 multicomboreadonlycolumn, multicomboshowselectallcheckbox 속성이 표시되지 않는 오류를 수정했습니다. |
| 97193 | 스크립트 내에서 큰 따옴표(")가 하나만 있는 경우(정규식 등의 사용으로) 해당 코드 뒤에 작성된 함수를 인식하지 못하는 오류를 수정했습니다. |
| 97041 | Grid 컴포넌트에 Dataset 오브젝트를 바인드 한 후 Dataset 오브젝트를 삭제하고 Undo 명령을 3회 이상 입력 시 에러 메시지 팝업이 표시되는 오류를 수정했습니다. |
| 96930 | Form Template 경로 변경 후 프로젝트를 내보낸 경우 다른 환경에서 프로젝트를 가져오기가 정상 실행되지 못하는 오류를 수정했습니다. |
| 96817 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "textareacontrol"인 셀이 많은 경우 Grid Contents Editor 실행 시 창이 늦게 표시되거나 비정상 종료되는 오류를 일부 개선했습니다. |
| 96696 | 스크립트 작성 시 switch 구문 내에서 let, const 변수 선언자 사용 시 에러로 처리되는 오류를 수정했습니다. |

## 14.5.6넥사크로 모듈 디벨로퍼

| RP | 설명 |
| --- | --- |
| 96595 | Deploy 시 경로를 변경한 후 다시 Deploy 할 때 이전 경로를 유지하지 못하는 오류를 수정했습니다. |

## 14.5.7기타

| RP | 설명 |
| --- | --- |
| 98163 | 최신 크롬 브라우저에서 안드로이드 기기 원격 디버깅이 동작하지 않는 오류를 수정했습니다(크롬 브라우저 118, 119 버전에서 발생한 오류입니다). |
| 98099 | 앱 실행 시 비정상 종료되는 오류를 수정했습니다(삼성 갤럭시 Z Fold 4 기기에서 펼친 상태로 앱 실행 시 발생한 오류입니다. 일부 앱에서 height 값을 정상적으로 처리하지 못해 발생한 오류입니다). |
| 97943 | 디스플레이 해상도가 1920x1200인 장비에서 앱 실행 시 흐릿하게 표시되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97654 | include 문을 사용해 가져온 js 파일 내에 "/* */" 형태의 주석이 포함된 경우 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 97576 | include 문을 사용하는 화면 로딩 시 TabpageControl 오브젝트의 연결된 Form의 레이아웃 선택 동작이 달라지는 오류를 수정했습니다. |
| 97401 | 크롬 확장 프로그램(Dark Reader) 실행 시 화면 로딩이 되지 않는 오류를 수정했습니다. |
| 97371 | 태블릿 기기에서 Desktop 스크린이 표시되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 브라우저를 데스크톱 모드로 실행하지 않은 경우 발생한 오류입니다). |
| 97338 | 화면 로딩 시 Application 오브젝트의 screenid 속성값을 잘못 처리하는 오류를 수정했습니다(안드로이드 운영체제에서 Edge 브라우저 실행 시 발생한 오류입니다). |

# 14.6도움말 수정 및 추가

| RP | 설명 |
| --- | --- |
| 9853598506 | WebView 컴포넌트 url 속성, callScript 메서드 설명에 크로스 도메인 상황에 대한 동작 변경 내용을 추가했습니다. |
| 98283 | VirtualFile 오브젝트의 open, read, write 메서드 실행 시 제약 사항에 대한 설명을 추가했습니다. |
| 98156 | View 컴포넌트 sourceview 속성에 대한 도움말 설명을 보완했습니다. |
| 97937 | Appendix > nexacro_config.xml 항목 중 log 설명을 수정했습니다. |
| 97604 | Environment, DataObject 오브젝트의 onerror 이벤트 설명을 보완했습니다. |
| 97496 | Dataset 오브젝트의 getDeletedRowCount, getDeletedRowset 메서드 설명을 보완했습니다. |
| 97492 | CheckBoxSet, MultiCombo 컴포넌트의 setSelectRange 메서드 설명 중 Syntax 표기에 오타가 있는 오류를 수정했습니다. |
| 97217 | Dataset 오브젝트의 setConstColumn 메서드 설명 중 잘못된 내용을 삭제했습니다. |
| 97671 | Static 컴포넌트의 usedecorate, GridCellControl, CellSubCellControl, ListViewCellControl 오브젝트의 displaytype 속성 설명에 특수문자 입력에 대한 제한 사항 설명을 추가했습니다. |

TOBESOFT ©