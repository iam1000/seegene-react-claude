---
title: "13.2024년 6월 (21.0.0.1700)"
source: "https://docs.tobesoft.com/product_information_nexacro_n_ko/05aec3499494f93f"
crawled_at: "2026-01-09T10:58:15.751Z"
---

# 13.2024년 6월 (21.0.0.1700)

Nexacro N / 모든 매뉴얼에서 검색제품 정보이전 릴리스 노트13.2024년 6월 (21.0.0.1700)article

# 13.2024년 6월 (21.0.0.1700)

## [Download PDF](https://docs.tobesoft.com/r/pdf/download_chapter/fa466c9bb1a71ea9/05aec3499494f93f)

playlist\_play

13.1버전정보

13.2릴리스 이후 수정 사항

13.2.121.0.0.1701

13.3신규기능

13.4변경기능

13.5오류수정

13.5.1기본 컴포넌트 / 오브젝트

Animation

Calendar

ChildFrame

Combo

DataObject

Dataset

Div

Edit

ExcelExportObject

ExcelImportObject

FileDialog

FileDownload/FileDownTransfer

Graphics

GoogleMap

Grid

ImageViewer

ListBox

ListView

MultiCombo

PopupDiv

PopupMenu

Radio

Static

Tab

Textarea

WebBrowser

WebView

XPush

기타 (컴포넌트 / 오브젝트)

기타 (transaction)

13.5.2접근성(Accessibility)

센스리더 사용 환경 오류

13.5.3Fluid Layout

13.5.4QuickCode

13.5.5넥사크로 스튜디오

13.5.6기타

13.6도움말 수정 및 추가

# 13.1버전정보

| Product Version | 21.0.0.1700 |
| --- | --- |
| File Version | 2024.6.28.1 |

- 사용 환경이나 브라우저 기능 차이 또는 제품 제약으로 발생할 수 있는 현상은 제품제약사항을 참고해주세요.
 
- 새로운 기능, 변경된 기능 목록에서 버전별 새로운 기능, 변경된 기능 목록을 확인할 수 있습니다.

# 13.2릴리스 이후 수정 사항

## 13.2.121.0.0.1701

| RP | 설명 |
| --- | --- |
| 100293 | Calendar 컴포넌트 텍스트 편집 영역에 포커스 이동 시 입력이 되지 못하는 오류를 수정했습니다(iOS 운영체제 WRE 실행 시 발생한 오류입니다. 블루투스 키보드를 연결해서 포커스를 이동하고 입력하는 경우 발생한 오류입니다. 21.0.0.1700 이후 버전에서 발생한 오류입니다). |
| 100290 | Combo 컴포넌트 텍스트 편집 영역에서 comboedit 영역에 텍스트 입력 시 문자 조합 중 반각으로 변경하고 입력해 combolist가 펼쳐지는 경우 이벤트가 발생하지 않거나 잘못된 순서로 처리되는 오류를 수정했습니다(IE11 또는 Edge 브라우저 IE 모드 실행 시 발생한 오류입니다. 21.0.0.1700 이후 버전에서 발생한 오류입니다). |
| 100287 | 넥사크로 스튜디오 속성창에서 Grid 컴포넌트의 autosizetype 속성값을 "row", displaytype 속성값을 "imagecontrol"로 설정하고 Dataset 오브젝트를 바인딩하는 경우 비정상 종료되는 오류를 수정했습니다(21.0.0.1700 이후 버전에서 발생한 오류입니다). |

# 13.3신규기능

1. **애플 앱스토어 정보 보호 정책을 대응하기 위해 보완 작업을 수행했습니다. (RP 99818)**
 

| 설명 |
| --- |
| iOS framework에 Privacy Manifest 파일을 추가했습니다.iOS framework에 디지털 서명을 추가했습니다. |

2. **Android NRE 환경에서 지원하는 통신 프로토콜을 추가했습니다. (RP 99391)**
 

| 설명 |
| --- |
| Android NRE 통신 프로토콜을 추가로 지원하도록 빌드 환경의 NDK 버전을 업데이트했습니다.추가된 통신 프로토콜은 "HTTP/2, TLS1.3, Proxy로 HTTPS 접근"입니다. |

3. **ListView 컴포넌트에서 oninput이 발생한 cell을 알 수 있는 기능을 추가했습니다. (RP 98578)**
 

| 설명 |
| --- |
| ListViewInputEventInfo를 추가했습니다.ListView > oninput 이벤트 정보를 변경 (InputEventInfo -> ListViewInputEventInfo) |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.EventInfo Objects > ListViewInputEventInfoComponents > Component > ListView > Event > oninput |

4. **DatePickerControl의 userstatus를 요일별로 지정할 수 있도록 기능을 추가했습니다. (RP 98515)**
 

| 설명 |
| --- |
| 추가한 userstatusmondaytuesdaywednesdaythursdayfriday |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Components > System Component > DatePickerControl > Objects > DatePickerBodyControl > Objects > DatePickerDayItemControlComponents > System Component > DatePickerControl > Objects > DatePickerBodyControl > Objects > DatePickerWeekItemControl |

5. **컴포넌트의 화면 좌표(x, y) 값을 얻을 수 있는 기능을 추가했습니다. (RP 98379)**
 

| 설명 |
| --- |
| 컴포넌트의 스크린 좌표(x, y) 값을 얻을 수 있습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.Misc. Objects > System > Method > getScreenPosition |

# 13.4변경기능

1. **실행 화면의 Zoom 값을 확인하는 Method의 지원 대상을 변경했습니다. (RP 98379)**
 

| 설명 |
| --- | --- |
| 변경 전 | NexacroAPI > Method > getWheelZoomWindows NRE만 지원 |
| 변경 후 | NexacroAPI > Method > getWheelZoomWindows NRE지원하는 모든 WRE 환경 추가 |

2. **해외 제품의 법인명을 변경했습니다. (RP 100213, 99396)**
 

| 설명 |
| --- | --- |
| 설치 위치는 변경하지 않았습니다. | |
| 변경 전 | NEXAWEB |
| 변경 후 | TOBESOFT |

3. **Android NRE 환경의 TargetSDK 지원 버전을 변경했습니다. (RP 99955, 99576, 99575, 99573, 99556, 99518)**
 

| 설명 |
| --- | --- |
| 변경 전 | TargetSDK 33 |
| 변경 후 | TargetSDK 34 |

4. **export 기능에 수정된 image base64 data URI Scheme 기능을 수정에 맞춰 매뉴얼 내용을 변경했습니다. (RP 99831)**
 

| 설명 |
| --- |
| * 해당 기능이 동작하려면 XENI 1.4.10 이상 버전을 사용해야 합니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.ExcelExportObject > Method > addExportItemstrExportImage"image" 설정 시, 설정된 이미지의 위치가 "http://" 또는 "https://"로 시작하는 인터넷 URL 경로이거나 "data"로 시작하는 Base 64 인코딩 데이터인 경우만 이미지를 그대로 Export 합니다. |

5. **\[접근성\] Radio 컴포넌트 접근 시 index(0)와 itemcount 속성값을 출력하지 않도록 변경했습니다. (RP 99696)**
 

| 설명 |
| --- | --- |
| * 센스리더 8.5.1 버전에서 확인했습니다. | |
| 변경 전 | 예시) 국내, 국외 선택구분(필수입력) 0 2 radio <-- 출력 |
| 변경 후 | 예시) 국내, 국외 선택구분(필수입력) radio <-- 출력 |

6. **QuickCode 사용 시 컴포넌트에 연결된 view.dataset을 form.dataset으로 통합하여 생성하도록 기능을 변경했습니다. (RP 99811, 99469, 99359, 99547)**
 

| 설명 |
| --- | --- |
| 변경 전 | QuickCode로 화면 구성 시 각 컴포넌트의 view.dataset을 생성합니다. |
| 변경 후 | QuickCode로 화면 구성 시 form.dataset 하나로 통합되어 생성합니다.변경 전에 작성된 QuickCode 프로젝트를 수정 없이 구동할 수 있도록 하위 호환성을 지원합니다.변경된 기능에 맞게 샘플 프로젝에 viewtemplate를 수정했습니다. |
| 자세한 내용은 "Nexacro N Reference Guide"를 확인하시기 바랍니다.View > Property > viewdatasetView > Property > sourceviewView > Method > getSourceViewView > Method > getViewDataset | |

7. **reload Method 사용 시 onbeforeclose, onclose 이벤트가 발생하지 않도록 기능을 변경했습니다. (RP 99020)**
 

| 설명 |
| --- |
| reload 사용 시 onbeforeclose, onclose 이벤트가 발생하지 않도록 기능을 변경했습니다.이벤트 버블이 변경됐으므로 기능 사용에 확인이 필요합니다. |

# 13.5오류수정

## 13.5.1기본 컴포넌트 / 오브젝트

### Animation

| RP | 설명 |
| --- | --- |
| 97887 | Animation 오브젝트의 loop 속성값이 true이고 loopcount 속성값을 1 이하의 값으로 설정한 경우 애니메이션이 반복되지 않고 일정 횟수 동작 후 멈추는 오류를 수정했습니다. |

### Calendar

| RP | 설명 |
| --- | --- |
| 99846 | Calendar 컴포넌트의 type 속성값이 "spin"이고 editformat 속성값이 "HH:mm" 형태인 경우 calendarspinupbutton, calendarspindownbutton 클릭 시 잘못된 값으로 처리되는 오류를 수정했습니다(21.0.0.1300 이후 버전에서 발생한 오류입니다). |
| 99382 | Calendar 컴포넌트의 autoskip 속성값을 true로 설정한 경우 날짜값을 직접 입력했을 때 유효하지 않은 날짜가 입력되어 보정되는 경우 autoskip 속성이 동작하지 못하는 오류를 수정했습니다. |
| 99263 | Calendar 컴포넌트 텍스트 편집 영역에 숫자 입력 시 잘못된 값으로 입력되는 오류를 수정했습니다(안드로이드 운영체제에서 데스크톱 모드로 브라우저 실행 시 발생한 오류입니다. 삼성 갤럭시 탭 S7에서 발생한 오류입니다). |
| 99254 | Calendar 컴포넌트의 dropbutton 클릭 시 datepicker가 표시되었다가 닫히는 오류를 수정했습니다(안드로이드 운영체제에서 데스크톱 모드로 브라우저 실행 시 발생한 오류입니다). |
| 99181 | Calendar 컴포넌트의 datepicker에서 날짜를 선택했을 때 oncloseup 이벤트 핸들러 함수 내에서 setSelect 메서드 실행 시 메서드가 정상적으로 동작하지 못하는 오류를 수정했습니다. |
| 99036 | Calendar 컴포넌트의 autoskip 속성값을 true로 설정한 상태에서 일본어 히라가나 입력 시 autoskip 속성이 적용되지 못하는 오류를 수정했습니다. |
| 98940 | Calendar 컴포넌트의 editformat 속성값에서 <Year> 값을 "yy"로 설정한 경우 기존 입력값이 없을 때 연도 앞자리가 "19"로 처리되는 오류를 수정했습니다. |
| 98913 | Calendar 컴포넌트의 dateformat, editformat 속성값이 "HH:mm"인 경우 calendaredit 영역에서 "mm" 위치에 범위를 벗어나는 값 입력 시 "HH" 위치에 값이 입력되는 오류를 수정했습니다. |
| 97374 | Calendar 컴포넌트에 숫자키 패드를 사용해 숫자 입력 시 유효하지 않은 값이 입력되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. 일본어 IME 사용 시 발생한 오류입니다). |
| 97342 | Calendar 컴포넌트의 dateformat, editformat 속성값이 "HH:mm"인 경우 calendaredit 영역에서 "HH:" 값을 선택한 상태에서 숫자 입력 시 "mm" 값에 바뀌는 오류를 수정했습니다. |
| 97329 | Calendar 컴포넌트의 autoskip 속성값이 true인 경우 텍스트 편집 영역의 입력값을 선택된 상태에서 백스페이스키 입력 시 autoskip이 동작하는 오류를 수정했습니다. |
| 97051 | Calendar 컴포넌트의 autoselect 속성값을 true로 설정하고 포커스를 이동한 상태에서 숫자 이외의 키를 입력하는 경우 선택 상태가 해제되면서 캐럿이 편집 영역 텍스트 마지막 위치로 이동하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 96463 | Calendar 컴포넌트의 dropbutton 선택 시 datepicker가 표시되면서 키패드가 올라오는 오류를 수정했습니다(Android, iOS 운영체제에서 실행 시 발생한 오류입니다. 21.0.0.1300 이후 버전에서 발생한 오류입니다). |

### ChildFrame

| RP | 설명 |
| --- | --- |
| 100199 | ontimer 이벤트 핸들러 함수 내에서 ChildFrame 오브젝트의 showModal 메서드를 실행해 창을 띄운 상태에서 바탕화면 보기, 숨기기 단축키(WIndows+D)를 입력해 창을 최소화했다가 다시 원래 상태로 전환했을 때 창이 다른 위치에 표시되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### Combo

| RP | 설명 |
| --- | --- |
| 99710 | alert 메서드 실행 후 Combo 컴포넌트의 combolist를 펼쳤을 때 현재 선택된 아이템과 상관없이 스크롤 위치가 최상단으로 표시되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 98937 | Combo 컴포넌트의 innerdataset으로 설정한 Dataset 오브젝트의 데이터를 transaction 메서드로 갱신한 경우 combolist가 표시되지 못하는 오류를 수정했습니다. |
| 98662 | Combo 컴포넌트의 type 속성값을 필터기능이 활성화되는 속성값으로 설정하고 일본어 키패드에서 입력한 값으로 필터링 목록이 표시되었을 때 Combo 컴포넌트를 다시 선택하면 필터링 이전 목록으로 돌아가지 못하는 오류를 수정했습니다(iPad 운영체제에서 실행 시 발생한 오류입니다. 21.0.0.1600 이후 버전에서 발생한 오류입니다). |

### DataObject

| RP | 설명 |
| --- | --- |
| 99916 | DataObject 오브젝트의 getResponseHeader 메서드 실행 시 드물게 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 996139960999608 | ProtocolAdaptor를 통한 DataObject 오브젝트 통신이 동작하지 못하는 오류를 수정했습니다(윈도우, 안드로이드, iOS 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97700 | Environment.httptimeout 속성값을 설정한 경우 DataObject 오브젝트 통신 시 httptimeout 속성에 설정한 값보다 통신 시간이 길어지는 경우 DataObjectErrorEventInfo 오브젝트의 statuscode 속성값이 10408이 아닌 0으로 반환되는 오류를 수정했습니다. |

### Dataset

| RP | 설명 |
| --- | --- |
| 99057 | Dataset 오브젝트의 saveJSON 또는 saveXML 메서드 호출 시 값 변환 방법 파라미터 값을 "exclude"로 설정한 경우 잘못된 값으로 처리되는 오류를 수정했습니다. |
| 98949 | Dataset 오브젝트의 Const Column 데이터 중 type이 "STRING"이 아닌 칼럼에 대해 setConstColumn 메서드로 값을 변경한 후 saveJSON 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 98902 | Dataset 오브젝트 데이터에 개행문자가 포함된 경우 loadJSON 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 98698 | Dataset 오브젝트의 canrowposchange 이벤트 핸들러 함수 내에서 transaction 메서드 실행 후 Grid 컴포넌트의 formatid 속성값 변경 시 반영되지 못하는 오류를 수정했습니다. |

### Div

| RP | 설명 |
| --- | --- |
| 100269 | Div 컴포넌트의 formscrolltype 속성값을 "both"로 설정하고 앱 실행 후 url 속성값을 변경한 후 formscrolltype 속성값을 "none"으로 변경했을 때 스크롤이 활성화되는 오류를 수정했습니다(iOS 운영체제에서 발생한 오류입니다). |
| 99072 | Div 컴포넌트의 onload 이벤트 핸들러 함수 내에서 url 속성값을 include 문이 포함된 Form 오브젝트로 설정하는 경우 스크립트 에러가 발생하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 99023 | Div 컴포넌트의 enable 속성값을 false로 변경 시 Div 컴포넌트 내 배치된 CheckBoxSet 컴포넌트의 enable 상태값이 변경되지 못하는 오류를 수정했습니다. |
| 98967 | Div 컴포넌트에 연결된 Form 오브젝트 영역에서 새로고침(Ctrl+F5) 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 21.0.0.1600 이후 버전에서 발생한 오류입니다). |

### Edit

| RP | 설명 |
| --- | --- |
| 99703 | Edit 컴포넌트의 inputmode 속성값을 "upper", inputtype 속성값을 "alpha, digit"로 설정하고 maxlength 속성값을 설정한 경우 실행 시 maxlength, inputmode 속성이 적용되지 못하는 오류를 수정했습니다(Android 운영체제 WRE 실행 시 발생한 오류입니다). |
| 99655 | Edit 컴포넌트의 inputtype 속성값이 "digit"인 경우 입력된 값을 선택하고 한글 입력 시도 시 입력된 값이 사라지는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99175 | Edit 컴포넌트의 setFocus 메서드 실행 후 value 속성값을 변경하는 동작을 빠르게 반복되는 경우 입력 동작이 정상 동작하지 못하는 오류를 수정했습니다(iOS 운영체제 NRE 실행 시 발생한 오류입니다. Correct Spelling Automatically 기능이 켜져 있는 경우 발생한 오류입니다). |
| 99104 | Edit 컴포넌트의 inputtype 속성값을 한글 입력이 안되도록 설정하고 텍스트 편집 영역에 영문 입력 후 한글 입력 시도 시 이전에 입력한 값이 사라지는 오류를 수정했습니다(윈도우 운영체제 11 버전에서 발생한 오류입니다).Edit 컴포넌트의 inputtype 속성값을 한글 입력이 안되도록 설정하고 텍스트 편집 영역에 영문 입력 후 한글 입력 시도 후 백스페이스 키 입력 시 바로 영문이 지워지지 않는 오류를 수정했습니다(윈도우 운영체제 10 버전에서 발생한 오류입니다). |
| 99064 | Edit 컴포넌트에 포커스가 있고 키패드가 올라온 상태에서 Form 영역 터치 시 키패드가 내려가는 오류를 수정했습니다(Android 운영체제에서 발생한 오류입니다). |
| 99028 | Edit 컴포넌트의 inputtype 속성값이 "number"인 경우 setFocus 메서드 실행으로 포커스 이동 시 숫자 키패드가 표시되지 못하는 오류를 수정했습니다(iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 98781 | 브라우저에서 개인 정보를 설정하고 Edit 컴포넌트에서 설정한 정보를 선택한 경우 포커스가 없는 다른 Edit 컴포넌트에도 같은 값이 입력되는 오류를 수정했습니다(Edge 브라우저에서 실행 시 발생한 오류입니다). |
| 97948 | Edit 컴포넌트의 oneditclick 이벤트 핸들러 함수 내에서 다른 Edit 컴포넌트의 setFocus 메서드 실행 시 이벤트가 발생한 Edit 컴포넌트의 displaynulltext 속성 설정값이 표시되지 못하는 오류를 수정했습니다. |
| 97778 | Edit 컴포넌트의 value 속성값에 바인딩된 값이 undefined인 경우 Edit 컴포넌트에 값 입력 후 Enter 키로 입력값을 확정하고 값을 지우고 다시 Enter 키로 입력값을 확정한 경우 Dataset 오브젝트의 RowType 값이 Dataset.ROWTYPE_NORMAL(1)로 처리되는 오류를 수정했습니다. |

### ExcelExportObject

| RP | 설명 |
| --- | --- |
| 99665 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "mask"이고 데이터 Row가 5000건 이상인 경우 ExcelExportObject 오브젝트의 exportData 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 99333 | Grid 컴포넌트 Cell 오브젝트의 display 속성값을 expr로 설정하고 Excel 함수로 데이터가 들어가는 경우 ExcelExportObject 오브젝트의 exportData 메서드 실행 시 생성된 Excel 파일 내 셀 서식이 "일반"이 아닌 "텍스트"로 설정되는 오류를 수정했습니다. |
| 98963 | ExcelExportObject 오브젝트의 exportDataEx 메서드 실행 시 텍스트 데이터가 숫자로 변환되는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 98501 | Grid 컴포넌트 Cell 오브젝트의 suppress 속성값을 bind 값으로 설정한 경우 ExcelExportObject 오브젝트의 exportData 메서드 실행 시 suppress가 적용되지 않는 오류를 수정했습니다. |

### ExcelImportObject

| RP | 설명 |
| --- | --- |
| 98731 | ExcelImportObject 오브젝트의 separator 속성값을 설정하고 importData 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |

### FileDialog

| RP | 설명 |
| --- | --- |
| 97578 | FileDialog 컴포넌트의 open 메서드 실행 시 constOpenMode 파라미터값이 "FileDialog.LOAD"일 때와 "FileDialog.MULTILOAD"일 때 표시되는 FileDialog가 달라지는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |

### FileDownload/FileDownTransfer

| RP | 설명 |
| --- | --- |
| 99158 | FileDownload 또는 FileDownTransfer 오브젝트의 download 메서드 실행 시 파일명이 잘못 설정되거나 파일을 내려받지 못하는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다). |

### Graphics

| RP | 설명 |
| --- | --- |
| 98702 | GraphicsImage 오브젝트의 image 속성값으로 설정한 이미지가 print 메서드 실행 시 출력되지 못하는 오류를 수정했습니다. |

### GoogleMap

| RP | 설명 |
| --- | --- |
| 99193 | GoogleMap 컴포넌트의 getCoordinates 메서드 실행 시 비정상 종료되는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |

### Grid

| RP | 설명 |
| --- | --- |
| 100370 | Grid 컴포넌트의 setRealRowSize 메서드 실행 후 height 속성값을 변경하는 경우 Row 높이가 메서드 실행 전 상태로 변경되는 오류를 수정했습니다. |
| 100045 | Grid 컴포넌트의 autosizingtype 속성값을 설정한 경우 setRealColSize 메서드 실행 시 적용되지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 99827 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "combo"인 경우 Grid 하단에 있는 항목 선택 시 combolist가 아래로 펼쳐지면서 일부 항목이 표시되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99653 | Grid 컴포넌트의 cellsizingtype 속성값을 "col" 또는 "both"로 설정하고 앱 실행 후 칼럼 너비를 마우스 드래그로 변경하고 autofittype 속성값을 "col"로 설정했다가 "none"으로 변경했을 때 원래 크기로 변경되지 못하는 오류를 수정했습니다. |
| 99436 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "treeitemcontrol"인 경우 selecttype 속성값 설정 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 9922498714 | Grid 컴포넌트 셀에 포커스가 있는 경우 Tab 키 입력 시 다음 셀로 이동하지 못하는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 99189 | Grid 컴포넌트의 autosizingtype 속성값이 "row"이고 Cell 오브젝트의 displaytype 속성값이 "imagecontrol"인 경우 테마에 포함되지 않은 이미지 파일 설정 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 99094 | Grid 컴포넌트의 autosizingtype 속성값을 "row", extendsizetype 속성값을 "row", fillareatype 속성값을 "linerow"로 설정한 상태에서 실행 시 가로, 세로 방향 스크롤바가 활성화된 경우 세로 방향으로 스크롤 이동하면 Row 크기가 변경되고 일부 데이터가 사라지는 오류를 수정했습니다. |
| 99029 | Grid 컴포넌트의 가로 스크롤 영역을 스와이프 동작으로 이동했을 때 행 단위로 이동하는 오류를 수정했습니다(iOS 운영체제에서 Safari 브라우저 실행 시 발생한 오류입니다. 21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 98985 | Grid 컴포넌트의 fillareatype 속성값이 "linerow"이고 트리 형태인 경우 트리를 접었을 때 Cell 오브젝트의 컨트롤 이미지가 남아있는 오류를 수정했습니다. |
| 98935 | Grid 컴포넌트의 autosizingtype 속성값이 "row" 또는 "both"인 경우 스크롤바의 trackbar를 사용해 스크롤을 맨 하단으로 이동했을 때 trackbar의 위치가 스크롤 영역 끝에서 떨어져 위치하는 오류를 수정했습니다(21.0.0.1300 이후 버전에서 발생한 오류입니다). |
| 98830 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "combocontrol"인 경우 text 속성값을 expr로 설정했을 때 적용되지 못하는 오류를 수정했습니다. |
| 98778 | Grid 컴포넌트의 scrollpixel 속성값을 "all"로 설정한 경우 스크롤바의 증가/감소 버튼을 사용하여 스크롤 시 Row 단위로 스크롤되지 않는 오류를 수정했습니다. |
| 98694 | Grid 컴포넌트에서 decbutton 또는 incbutton을 클릭해서 스크롤 이동 시 일정 간격으로 이동하지 못하는 오류를 수정했습니다. |
| 98692 | Grid 컴포넌트의 selecttype 속성값을 "area"로 설정한 경우 바인딩된 Dataset 오브젝트의 canrowposchange 이벤트 핸들러 함수 내에서 alert 메서드 실행 시 비정상 종료되는 오류를 수정했습니다. |
| 98504 | Grid 컴포넌트의 mergeCell 메서드 실행 시 작업 완료까지 시간이 많이 걸리는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 97698 | Grid 컴포넌트 head 영역 Cell 오브젝트의 edittype 속성값 설정 후 Cell 영역 터치 시 편집 상태로 전환되지 못하는 오류를 수정했습니다(Android, iOS 운영체제 WRE 실행 시 발생한 오류입니다). |
| 97319 | Grid 컴포넌트의 autosizingtype 속성값은 "col", autosizebandtype 속성값은 "allband" 또는 "head"로 설정한 경우 head cell에 설정한 padding 속성이 적용되지 못하는 오류를 수정했습니다. |

### ImageViewer

| RP | 설명 |
| --- | --- |
| 99643 | ImageViewer 컴포넌트의 width, height 속성값이 모두 0인 경우 system.print 메서드 실행 시 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99530 | ImageViewer 컴포넌트의 stretch 속성값을 "fixaspectratio"로 설정한 경우 이미지 해상도가 떨어지는 오류를 수정했습니다(Android 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99049 | ImageViewer 컴포넌트의 stretch 속성값이 "fit" 또는 "fixaspectratio"인 경우 메모리가 증가하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### ListBox

| RP | 설명 |
| --- | --- |
| 98789 | ListBox 컴포넌트의 multiselect 속성값이 true인 경우 cssclass 속성값 변경으로 border 속성이 변경되면 multiselect가 해제되는 오류를 수정했습니다. |

### ListView

| RP | 설명 |
| --- | --- |
| 99919 | ListView 컴포넌트 Cell 오브젝트의 edittype 속성값을 expr로 설정하고 textareaacceptsenter 속성값을 true로 설정한 경우 Cell 편집 시 Enter 키 입력으로 개행 처리가 되지 못하는 오류를 수정했습니다. |
| 99230 | ListView 컴포넌트 Cell 오브젝트의 edittype 속성값을 expr로 설정한 상태에서 "textarea"로 반영됐을 때 CTRL+ENTER 키 입력 시 줄 바꿈이 안 되는 오류를 수정했습니다. |
| 99047 | ListView 컴포넌트의 bandexpandtype 속성값이 "accordion"인 경우 bodyband의 expandbar 아이콘이 현재 상태를 표시하지 못하는 오류를 수정했습니다. |
| 99015 | ListView 컴포넌트에 보이는 Row가 실제 Row보다 적은 경우 첫 번째 Row의 Detail 밴드를 펼친 상태에서 아래 방향으로 스크롤 이동 시 펼치지 않은 Row의 expandbar 버튼이 Detail 밴드가 펼쳐진 상태로 표시되는 오류를 수정했습니다. |
| 98649 | ListView 컴포넌트 Cell 오브젝트 편집 상태에서 Detail Band 확장 단축키(Ctrl+Alt+E) 입력 시 동작하지 못하는 오류를 수정했습니다. |
| 98551 | ListView 컴포넌트 oncellclick 이벤트 핸들러 함수 내에서 trackPopupByComponent 메서드를 실행하는 경우 스크롤 위치가 변경되어 팝업창 위치가 잘못 표시되는 오류를 수정했습니다(Row가 1개인 경우에만 스크롤 위치를 변경하지 않도록 수정했습니다). |

### MultiCombo

| RP | 설명 |
| --- | --- |
| 99805 | MultiCombo 컴포넌트의 enable 속성값을 false에서 true로 변경한 후 multicombotext 영역 클릭 시 multicombolist가 표시되지 못하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 99804 | MultiCombo 컴포넌트의 enable 속성값을 false에서 true로 변경한 후 Tab 키 입력으로 포커스를 정상적으로 이동하지 못하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 99332 | MultiCombo 컴포넌트의 autoselect 속성값을 true로 설정했을 때 반영되지 못하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 99112 | MultiCombo 컴포넌트의 multicombolist 컨트롤에 항목이 많을 때 스크롤바가 표시되지 않거나 일부 checkboxsetitem이 표시되지 않거나 스크롤 시 스크립트 에러가 발생하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |

### PopupDiv

| RP | 설명 |
| --- | --- |
| 99529 | 키패드가 열려있는 상태에서 PopupDiv 컴포넌트를 띄우는 경우 키패드가 내려가면 PopupDiv 컴포넌트도 사라지는 오류를 수정했습니다(Android 운영체제 네이버 앱에서 실행할 경우 발생한 오류입니다). |
| 97207 | 마우스 휠 확대/축소 기능을 사용해 화면 크기를 변경한 후 PopupDiv 컴포넌트의 trackPopupByComponent 메서드 실행 시 컴포넌트 크기가 제대로 적용되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### PopupMenu

| RP | 설명 |
| --- | --- |
| 99124 | PopupMenu 컴포넌트의 trackPopupByComponent 메서드를 실행해 PopupMenu 컴포넌트가 표시된 상태에서 다른 애플리케이션으로 포커스를 이동했다가 Edit 컴포넌트로 포커스 이동했을 때 한글 IME에서 한영전환 동작을 하지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 98923 | PopupMenu 스타일 관련 아래와 같은 오류를 수정했습니다.CSS로 padding-left 설정 시 padding-right에 적용되는 오류CSS에서 설정한 PopupMenu 스타일 속성이 popupmenuitem에 적용되는 오류속성값으로 지정한 경우 padding 속성이 적용되지 못하는 오류CSS로 padding-right 설정 시 하위 레벨 메뉴에는 적용되지 않고 표시되는 오류 |

### Radio

| RP | 설명 |
| --- | --- |
| 98521 | Radio 컴포넌트 RadioItemControl의 -nexa-icon 속성값을 테마가 아닌 ImageResource의 값으로 설정하면 이미지 크기가 잘못 처리되는 오류를 수정했습니다(21.0.0.1500 이후 버전에서 발생한 오류입니다). |

### Static

| RP | 설명 |
| --- | --- |
| 99542 | Static 컴포넌트의 usedecorate 속성값을 true로 설정한 경우 text 속성으로 설정한 값이 Decorate 문법에 맞지 않은 경우 getDisplayText 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 99439 | Static 컴포넌트의 usedecorate 속성값을 true로 설정한 경우 text 속성값에 "&lt;"가 포함된 경우 "&lt;" 다음에 오는 텍스트에 지정한 효과가 적용되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### Tab

| RP | 설명 |
| --- | --- |
| 99046 | Tab 컴포넌트의 insertTabpage 메서드 실행 시 width 속성값에 따라 추가한 tabbuttonitem이 표시되지 못하는 오류를 수정했습니다. |
| 99026 | TabpageControl 오브젝트에 연결된 Form 오브젝트의 onload 이벤트 핸들러 함수 내에서 Form 오브젝트의 height 속성값이 0으로 처리되는 오류를 수정했습니다. |

### Textarea

| RP | 설명 |
| --- | --- |
| 97155 | TextArea 컴포넌트의 width 속성값을 설정하지 않고 left, right 속성값만 설정한 경우 Form 오브젝트 크기 변경 시 화면이 바로 갱신되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### WebBrowser

| RP | 설명 |
| --- | --- |
| 98793 | 화면 내에 WebBrowser 컴포넌트가 2개 이상 있는 경우 WebBrowser 컴포넌트 내 로딩된 콘텐츠 영역에서 휠 스크롤 동작으로 커서 위치가 WebBrowser 컴포넌트 영역을 벗어나는 경우 휠 스크롤이 동작하지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |

### WebView

| RP | 설명 |
| --- | --- |
| 99349 | WebView 컴포넌트의 onusernotify 이벤트 핸들러 함수 내에서 showModal 메서드 실행 시 팝업창이 늦게 표시되거나 표시되지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99337 | WebView 컴포넌트가 2개 이상이고 같은 콘텐츠를 로딩하는 경우 onusernotify 이벤트가 하나의 WebView 컴포넌트에서만 발생하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 98787 | WebView 컴포넌트에서 로딩한 콘텐츠에서 alert 창을 띄운 상태에서 showModal 메서드를 실행하면 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |

### XPush

| RP | 설명 |
| --- | --- |
| 100150 | XPush 오브젝트의 connect 메서드 실행 시 iplist 속성값으로 잘못된 IP가 설정된 경우 비정상 종료되는 오류를 수정했습니다(macOS 운영체제 NRE 실행 시 발생한 오류입니다). |

### 기타 (컴포넌트 / 오브젝트)

| RP | 설명 |
| --- | --- |
| 100149 | 화면 확대 시 컴포넌트 내 포함된 icon 이미지 가장자리가 부드럽게 표시되지 못하는 보이는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100121100116 | Calendar, MaskEdit 컴포넌트의 텍스트 편집 영역에서 마우스 드래그 동작으로 텍스트 선택 시 padding 영역에서 시작하는 경우 텍스트가 선택된 상태에서 값 입력 시 캐럿 위치가 잘못 처리되면서 입력한 값과 다른 값이 입력되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 100028 | nexacro.open 메서드 실행으로 Modeless 창을 띄운 상태에서 부모 Form 오브젝트의 reload 메서드 실행 시 스크립트 에러가 발생하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99704 | system.showModalWindow 메서드 실행으로 Modal 창을 띄운 상태에서 창을 최대화했다가 원래 크기로 되돌리지 못하는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99654 | nexacro.open 메서드를 실행해 띄운 Form 오브젝트 내 TextArea 컴포넌트의 컨텍스트 메뉴가 표시되지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 99438 | inputtype 속성값이 "number"이고 inputfilter 속성값이 "dot,comma,space,sign,symbol"인 Edit 컴포넌트 또는 MaskEdit 컴포넌트에 숫자 입력 후 "…" 입력 시 이전에 입력한 숫자가 삭제되는 오류를 수정했습니다(iOS 운영체제에서 Smart Punctuation 기능을 활성화한 상태에서 실행 시 발생한 오류입니다). |
| 99328 | 컴포넌트의 좌표 속성값 중 기준 컴포넌트를 참조하도록 설정한 속성과 비율 단위(%)로 설정한 속성이 포함되어 있고 right 속성값을 설정한 경우 해당 컴포넌트가 화면 표시 영역 외에 표시되면서 영역 외 부분이 스크롤바 사이즈 이내에 표시되면 Form 오브젝트 로딩 또는 resetScroll 메서드 실행 시 스크롤바가 잘못 표시되거나 컴포넌트가 잘못된 크기로 표시되는 오류를 수정했습니다. |
| 99268 | 팝업창 내 WebBrowser, VideoPlayer 컴포넌트가 포함된 경우 팝업창을 닫을 때 화면이 멈추는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99040 | Div 컴포넌트의 scrolltype 속성값을 "none"으로 설정한 상태에서 스크롤이 활성화되는 오류를 수정했습니다(Android, iOS 운영체제에서 발생한 오류입니다). |
| 98794 | system.saveToImageFile 메서드 실행 시 파일명(strFileName) 파라미터값을 빈 값("")으로 설정하면 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 98668 | Div, PopupDiv, View, TabpageControl에서 formscrolltype, formscrollbartype 속성값 설정 시 연결된 Form 오브젝트의 scrolltype, scrollbartype 속성값이 변경되는 오류를 수정했습니다.Div, PopupDiv, View, TabpageControl에 연결된 Form 오브젝트 변경 시 formscrolltype, formscrollbartype 속성값이 변경되는 오류를 수정했습니다.Div 컴포넌트의 formscrolltype 속성값이 "none"인 경우 Tab키 입력 시 컴포넌트로 포커스가 이동하지 못하는 오류를 수정했습니다. |
| 96958 | 오브젝트의 showtitleicon 속성값을 false로 설정했을 때 적용되지 못하는 오류를 수정했습니다. |

### 기타 (transaction)

| RP | 설명 |
| --- | --- |
| 99614 | Environment 오브젝트의 httpretry 속성값을 1 이상의 값으로 설정한 경우 transaction 메서드 실행 시 통신이 실패해 재시도되는 경우 비정상 종료되는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다). |
| 99111 | transaction 메서드 실행 시 통신 에러가 발생한 경우 데이터가 업데이트되는(RowType이 변경되는) 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다. ErrorMsg 다음에 ErrorCode가 반환되는 경우 발생한 오류입니다).NRE 실행 시 발생한 오류는 21.0.0.1600 RP 98463에서 수정됐습니다. |
| 99043 | transaction 메서드 실행 시 nDataType 파라미터 값이 "3"이고 strArgument 파라미터 값에 "\" 문자가 포함된 경우 데이터 수신 시 스크립트 에러가 발생하는 오류를 수정했습니다. |
| 98950 | transaction 메서드 실행 시 칼럼 정보가 없는 데이터를 수신하는 경우 데이터 처리 에러가 발생하는 오류를 수정했습니다(NRE 실행 시 발생한 오류입니다). |
| 98581 | gzip 인코딩 된 JSON 파일 처리 시 스크립트 에러가 발생하는 오류를 수정했습니다(윈도우, Android 운영체제 NRE 실행 시 발생한 오류입니다). |
| 98338 | transaction 메서드 실행 시 bAsync 파라미터값을 false로 설정한 경우 nexacro.setHTTPHeaderVariable 메서드로 설정한 변숫값이 기본값에 누적되어 처리되는 오류를 수정했습니다. |

## 13.5.2접근성(Accessibility)

| RP | 설명 |
| --- | --- |
| 100107 | Radio 컴포넌트의 accessibilityenable 속성값은 false이고 itemaccessibilityenable 속성값은 true인 경우 상하 방향키로 radioitem으로 이동하지 못하는 오류를 수정했습니다. |
| 100088 | Edit, Calendar 컴포넌트의 accessibilitylabel 속성값을 설정하지 않았을 때 컴포넌트의 name, value, text 속성값이 accessibilitylabel 속성으로 적용되지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 99928 | Shift+Tab 키를 동시에 누른 상태에서 Shift 키가 먼저 올라가는 경우 Tab 키 onkeydown 이벤트가 발생하는 오류를 수정했습니다. |
| 99772 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "treeitemcontrol"인 경우 해당 셀 접근 시 트리의 상태값을 읽지 못하는 오류를 수정했습니다. |
| 99624 | Tab 컴포넌트의 tabbuttonitem이 화면에 보이는 것보다 많은 경우 방향키 입력 시 보이지 않는 tabbuttonitem으로 이동하지 못하는 오류를 수정했습니다. |
| 99540 | Dataset 오브젝트의 loadCSV 메서드 실행 후 Grid 컴포넌트의 createFormat 메서드 실행 시 로딩 시간이 오래 걸리는 오류를 수정했습니다. |
| 99525 | Static 컴포넌트의 usedecorate 속성값을 true로 설정한 경우 text 속성으로 설정한 값이 Decorate 문법에 맞지 않은 경우 스크립트 에러가 발생하고 화면이 표시되지 못하는 오류를 수정했습니다. |
| 99151 | Tab 컴포넌트의 tabbuttonitem이 화면에 보이는 것보다 많은 경우 전체 tabbuttonitem 숫자를 잘못 읽는 오류를 수정했습니다. |
| 98643 | ListView 컴포넌트 Cell 오브젝트의 edittype 속성값이 "calendar" 또는 "combo"인 경우 값을 변경했을 때 변경된 값을 읽지 못하는 오류를 수정했습니다. |

### 센스리더 사용 환경 오류

접근성 관련 오류 중 센스리더 사용 환경에서 가상 커서를 사용으로 설정하고 구글 크롬 브라우저 실행 시 발생한 오류를 따로 분류해 정리합니다.

센스리더 사용 환경이 다른 경우에는 실행 환경을 별도 기재했습니다.

| RP | 설명 |
| --- | --- |
| 100125 | Tab 컴포넌트의 focusacceptable 속성값이 true이고 showextrabutton 속성값이 true인 경우 상하방향키로 tabbutton 이동 시 마지막 tabbutton이 잘못된 위치에 표시되는 오류를 수정했습니다. |
| 99883 | Grid 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "checkboxcontrol"이고 edittype 속성값이 "checkbox"인 경우 스페이스바를 눌렀을 때 checkbox 선택상태가 변경되지 못하는 오류를 수정했습니다. |
| 99868 | 컴포넌트의 accessibilityenable 속성값을 false로 변경 후 true로 변경한 경우 가상커서가 이동하지 못하는 오류를 수정했습니다. |
| 99815 | Static 컴포넌트로 초점을 이동하지 못하는 오류를 수정했습니다(센스리더 8.5 버전 사용 시 발생한 오류입니다). |
| 99623 | Tab 컴포넌트의 prevbutton, nextbutton이 활성화된 경우 해당 버튼 컨트롤에 초점이 이동하는 오류를 수정했습니다. |
| 98853 | ListView 컴포넌트에서 아래 방향키 입력 시 순서대로 가상커서가 이동하지 못하는 오류를 수정했습니다. |
| 9864798644 | ListView 컴포넌트 Cell 오브젝트의 displaytype 속성값이 "imagecontrol"인 경우 방향키로 이동하지 못하는 오류를 수정했습니다. |
| 9864298638 | ListView 컴포넌트 내에서 상하 방향키 입력 시 가상커서가 순서대로 이동하지 못하는 오류를 수정했습니다. |
| 98640 | ListView 컴포넌트 Cell 오브젝트의 displaytype 속성값을 "checkboxcontrol", edittype 속성값을 "checkbox"로 설정한 경우 체크 상태 변경 시 변경된 상태 정보를 읽지 못하는 오류를 수정했습니다. |
| 98630 | TextArea 컴포넌트의 readonly 속성값이 true인 경우 TextArea 컴포넌트로 이동하지 못하는 오류를 수정했습니다. |
| 98620 | ImageViewer 컴포넌트의 accessibilitydesclevel 속성값을 "none"으로 설정한 경우 role 정보를 읽는 오류를 수정했습니다. |
| 98614 | 방향키 입력으로 Div 컴포넌트 이동 시 "빈 줄"이라고 읽는 오류를 수정했습니다. |
| 98607 | Button 컴포넌트의 text 속성값을 스크립트로 설정한 경우 Button 컴포넌트로 이동 시 text 속성값을 2번 읽는 오류를 수정했습니다. |
| 98590 | Grid 컴포넌트 Cell 오브젝트의 edittype 속성값이 "combo"인 경우 role 정보를 읽지 못하는 오류를 수정했습니다. |
| 98574 | Form 오브젝트 또는 Form 오브젝트를 포함한 컴포넌트 진입 시 Form 정보를 읽는 오류를 수정했습니다. |
| 98561 | TextArea 컴포넌트의 readonly 속성값이 true인 경우 방향키로 이동하지 못하는 오류를 수정했습니다. |
| 97157 | Div 컴포넌트의 semantictag 속성값 설정 시 semantictag 정보를 읽지 못하는 오류를 수정했습니다. |
| 97002 | 텍스트 편집 영역을 가지고 있는 컴포넌트가 문서 끝에 위치한 경우 문서 끝으로 초점 이동 시 이전에 읽은 컴포넌트 label을 읽는 오류를 수정했습니다. |

## 13.5.3Fluid Layout

| RP | 설명 |
| --- | --- |
| 99476 | Div 컴포넌트의 Layout 오브젝트 type 속성값이 "horizontal" 또는 "vertical"인 경우 내부에 배치된 Div 컴포넌트의 border 속성값이 표시되지 못하는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |

## 13.5.4QuickCode

| RP | 설명 |
| --- | --- |
| 100218 | Action 오브젝트 삭제 시 스크립트 에러가 발생하는 오류를 수정했습니다. |

## 13.5.5넥사크로 스튜디오

| RP | 설명 |
| --- | --- |
| 100240 | Radio 컴포넌트 RadioItemControl의 -nexa-icon 속성값을 테마가 아닌 ImageResource의 값으로 설정하면 디자인 창에서 radioitem text 일부가 잘려서 표시되는 오류를 수정했습니다. |
| 100157 | 넥사크로 14 프로젝트 마이그레이션 진행 시 중첩된 컨테이너 컴포넌트 내 바인딩 정보가 잘못 변환되는 오류를 수정했습니다. |
| 99557 | Project Explorer에서 ProtocolAdaptors 항목 등록 시 서비스 대상으로 "Web Browser" 항목을 선택한 경우 동작하지 못하는 오류를 수정했습니다. |
| 99404 | 스크립트 작성 시 switch 구문 내에서 같은 const 변수 선언자를 다른 case문에 사용 시 에러로 처리되는 오류를 수정했습니다. |
| 99403 | 스크립트 작성 시 switch 구문 내에서 Block scope 없이 const 변수 선언자를 사용 시 에러로 처리되는 오류를 수정했습니다. |
| 99197 | Ctrl 키를 누른 상태에서 1개 이상의 컴포넌트를 선택하고 TabpageControl 영역 선택 시 비정상 종료되는 오류를 수정했습니다(넥사크로 스튜디오 64비트 버전 실행 시 발생한 오류입니다). |
| 98814 | 스크립트 내에 "/" 문자가 포함된 경우 넥사크로 스튜디오 내부 기능 동작 시 해당 문자 이후의 함수를 찾지 못하는 오류를 수정했습니다(21.0.0.1600 이후 버전에서 발생한 오류입니다). |
| 98672 | 액티브 X 설치 시 설치 경로명 대소문자가 다르게 설정되어 리눅스 환경에서 Nexacro Deploy JAVA 실행 시 정상 처리되지 못하는 오류를 수정했습니다.넥사크로 스튜디오 64비트 버전에서 64비트 액티브 X가 목록에 표시되지 못하는 오류를 수정했습니다. |
| 98569 | [] 기호 안에 "obj.id" 형태의 코드가 포함된 경우 compress 처리 시 잘못된 기호가 포함되는 오류를 수정했습니다. |

## 13.5.6기타

| RP | 설명 |
| --- | --- |
| 998399697199689 | 구현 항목이 노출되지 않은 것을 수정했습니다.TitleBarControl > property > titleiconNotificationEventInfo > Property > messages |
| 99369 | 스마트 TV 내장 브라우저에서 마우스 클릭 이벤트가 발생하지 못하는 오류를 수정했습니다(삼성 스마트 TV 타이젠 브라우저에서 실행 시 발생한 오류입니다). |
| 99325 | 디바이스 방향을 가로 방향에서 세로 방향으로 전환 후 showModal 메서드 실행 시 모달창이 지정한 위치에 표시되지 못하는 오류를 수정했습니다(Android 운영체제에서 삼성 브라우저 실행 시 발생한 오류입니다. 21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 99324 | 디바이스 방향을 가로 방향에서 세로 방향으로 전환했을 때 화면 크기를 잘못 계산하는 오류를 수정했습니다(Android 운영체제에서 삼성 브라우저 실행 시 발생한 오류입니다. 21.0.0.1500 이후 버전에서 발생한 오류입니다). |
| 99199 | 특정 ActiveX Control 추가한 화면 실행 시 간헐적으로 멈추는 오류를 수정했습니다(윈도우 운영체제 NRE 실행 시 발생한 오류입니다. 특정 OCX 파일 설정 시 발생한 오류입니다). |
| 99138 | 앱을 실행하지 못하는 오류를 수정했습니다(Android 운영체제 버전 5 기기에서 NRE 실행 시 발생한 오류입니다). |
| 99081 | UserFont를 적용한 경우 앱 로딩 시 에러가 발생하는 오류를 수정했습니다(iOS 운영체제 17 이상 버전 WRE 실행 시 발생한 오류입니다). |
| 98783 | 애플리케이션 실행 시 어베스트(Avast) 안티바이러스 제품에서 Framework.js 내 일부 코드를 보안 취약점(JS:Pdfka-DL [Expl])으로 인식하는 오류를 수정했습니다. |
| 98770 | 앱 실행 시 SecurityException으로 비정상 종료되는 오류를 수정했습니다(안드로이드 운영체제 NRE 실행 시 발생한 오류입니다. 안드로이드 14 이상 버전에서 발생한 오류입니다). |
| 98442 | 앱 실행 시 화면이 깨지는 오류를 수정했습니다(삼성 갤럭시 Z Fold 4 기기에서 펼친 상태로 앱 실행 후 접었을 때 발생한 오류입니다). |
| 97503 | 모달창을 띄운 후 브라우저에서 새 탭을 열고 브라우저 크기를 최대화한 후 새 탭을 닫으면 모달창 위치가 잘못된 곳으로 설정되는 오류를 수정했습니다(WRE 실행 시 발생한 오류입니다). |
| 96978 | 미구현 항목이 잘못 노출된 것을 수정했습니다.아래 항목을 삭제했습니다.ChildFrame > Property > taskbaricon |

# 13.6도움말 수정 및 추가

| RP | 설명 |
| --- | --- |
| 99953 | Dataset 오브젝트의 extractRows, extractRowsNF 메서드 설명에 다른 작업을 위한 메서드명이 잘못 기재된 것을 수정했습니다. |
| 99826 | 컴포넌트의 letter-spacing 속성 설명에서 단위 표기에 대한 설명을 보완했습니다. |
| 99809 | Dataset 오브젝트의 loadBIN 메서드 설명 중 코드 오타를 수정했습니다. |
| 99289 | ExcelExportObject 오브젝트의 addExportItem 메서드 설명을 보완했습니다. |
| 99090 | Dataset 오브젝트의 extractRows, extractRowsNF 메서드 설명을 보완했습니다. |
| 98951 | Calendar 컴포넌트 editformat 속성에 대한 설명 중 "yy" 설정값에 대한 설명을 보완했습니다. |
| 98925 | ProgressBar 오브젝트의 step 속성 설명 중 Syntax 예제 오타를 수정했습니다. |

TOBESOFT ©